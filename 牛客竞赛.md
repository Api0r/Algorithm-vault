
# 区间 | 线段 重合 - 贪心 & 二分 + 差分
[D-小红的最佳区间_牛客周赛 Round 143](https://ac.nowcoder.com/acm/contest/134529/D)
![](image/Pasted%20image%2020260510221449.png)

## 贪心 + 二分
贪心的想，我们可以**枚举** 每个给定区间的右端点，以这个点为起点向右扩展K，查看这个区间内有多少个相交区间，每次查找的答案取最大就是我们的结果了
- 显然，我们查找的时间复杂度要log
- 很容易想到二分，并且我们枚举的区间端点也是需要排序的
- 那么我们可以将每个区间的左右端点都放到各自的数组中， 然后进行排序

关键是如何找到有多少个区间呢
- 设我们的范围区间是[L, R]
- 我们要求：有多少个区间满足：其左端点 l ≤ R 且 右端点 r ≥ e [i] ？ 
- 显然，我们知道r >= e[i] 的下界，即我们枚举的下标i
- 那么我们只需要找到上界 l <= R ，这可以二分左端点数组找到
```c++
void ap() {
    int n, k; cin >> n >> k;
    vector<int> s, e;
    for (int i = 0; i < n; i++) {
        int l, r; cin >> l >> r;
        s.push_back(l), e.push_back(r);
    }
     
    sort(s.begin(), s.end());
    sort(e.begin(), e.end());
     
    int ans = 1;
    for (int i = 0; i < n; i++) {
        int L = e[i];
        int R = L + k;
        
        auto p = lower_bound(s.begin(), s.end(), R + 1) - s.begin() - 1;
        
        ans = max(ans,  p - i + 1);
    }
    cout << ans;
     
    return ;
}
```

## 差分

我们可以可视化，固定一个长度为k的 山洞，将给定的数组看成是车辆，以恒定的速度向左边往山洞里边走。
问题变为：最多有多少辆车同时在山洞里边?

我们可以将每辆车向左加k个长度，
问题变为：一个点最多有多少辆车重叠？
	或者说：重叠区间的数量最大是多少？

看到这个问题，可以利用差分解决

![](image/Pasted%20image%2020260511140532.png)
```c++
void ap() {
    int n, k; cin >> n >> k;
    map<int ,int> d;
    for (int i = 0; i < n; i++) {
        int l, r; cin >> l >> r;
        d[l - k]++;
        d[r + 1]--;
    }
     
    int s = 0;
    int ans = 1;
    for (auto & [x, v] : d) {
        s += v;
        ans = max(ans, s);
    }
     
    cout << ans;
     
    return ;
}
```

# 位运算 | 同余，组合数
[D-骗你的，其实我是小红_牛客周赛 Round 144](https://ac.nowcoder.com/acm/contest/134957/D)
![](image/Pasted%20image%2020260518152346.png)
```c++
void ap() {
	int l, r, k; cin >> l >> r >> k;
    
    int n = r - l + 1;
    int t = n / k, e = n % k;
    
    auto C = [&](int x) {return x * (x - 1) / 2;};//c(x, 2)
    
    //(同余的数中)有e个数会出现t + 1次， 剩下的k - e 个数出现t次
    //每个数 任取两个 就可以构成一个队，即C(x, 2)
    cout << e * C(t + 1) + (k - e) * C(t) << endl;
	return ;
}
```

![](image/Pasted%20image%2020260518153143.png)
一个数字x 是 k 的倍数， 那么有x % k = 0
即x 的 低k位 都为0(二进制来看)

x = i ^ j
所以i ，j 的 低k位 都相等

那么就有 i % k = j % k

因此我们只需要看长度为n中， 周期为k， 有多少个同余的数，每个同余的数看成一起，任取两个就是这组同余的可以组成的二元组的数量

一个周期k 内， 有k个数，每个数余数都不相同，再看剩下的 一个的不完整的周期


# 动态规划 、质因数分解 、 路径还原
[D-小红的子序列_牛客周赛 Round 147](https://ac.nowcoder.com/acm/contest/136224/D)
![](image/Pasted%20image%2020260608122903.png)


# Dijkstra + 构造

[E-小橙分配边权_牛客小白月赛134](https://ac.nowcoder.com/acm/contest/137072/E)

**知识点**：图论、最短路性质、构造

若存在源点 $s$，则必须有 $a_s=0$。对任意边 $(u,v)$，由于最短路距离分别为 $a_u,a_v$，边权至少需要满足 $w(u,v)\ge |a_u-a_v|$。因此若直接令每条边权为 $|a_u-a_v|$，任意路径长度都不会小于终点的 $a$ 值。

接下来只需要保证每个点都能从某个 $a_s=0$ 的点出发，沿着 $a$ 值不下降的路径到达。这样路径长度会发生望远镜求和，恰好为 $a_i$。

若不存在这样的零点或存在点不可达，则无解；否则输出每条边的 $|a_u-a_v|$ 即可。

时间复杂度 $\mathcal{O}(n+m)$​

```c++
void ap() {
	int n, m; cin >> n >> m;
    vector<int> a(n);
    for (int & i : a) cin >> i;
    
    vector<vector<pair<int, int>>> g(n);
    vector<int> w(m);
    for (int i = 0; i < m; i++) {
        int u, v; cin >> u >> v;
        u--, v--;
        
        w[i] = abs(a[u] - a[v]);
        g[u].push_back({v, w[i]});
        g[v].push_back({u, w[i]});
    }
    
    int s = -1;
    for (int i = 0; i < n; i++) {
        if (a[i] == 0) s = i;
    }
    
    if (s == -1) {
        cout << -1 << endl;
        return;
    }
    
    vector<int> dis(n, 2e18);
    // 堆中保存 (起点到节点 x 的最短路长度，节点 x)
    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<>> pq;
    dis[s] = 0; // 起点到自己的距离是 0
    pq.emplace(0, s);

    while (!pq.empty()) {
        auto [dis_x, x] = pq.top();
        pq.pop();
        if (dis_x > dis[x]) { // x 之前出堆过
            continue;
        }
        for (auto& [y, wt] : g[x]) {
            auto new_dis_y = dis_x + wt;
            if (new_dis_y < dis[y]) {
                dis[y] = new_dis_y; // 更新 x 的邻居的最短路
                // 懒更新堆：只插入数据，不更新堆中数据
                // 相同节点可能有多个不同的 new_dis_y，除了最小的 new_dis_y，其余值都会触发上面的 continue
                pq.emplace(new_dis_y, y);
            }
        }
    }

    for (int i = 0; i < n; i++) {
        if (dis[i] != a[i]) {
            cout << -1 << endl;
            return;
        }
    }
    
    for (int i = 0; i < m; i++) {
        cout << w[i] << endl;
    }
    
	return ;
}
```

# 二维差分、切比雪夫距离、枚举中心

[F-小橙协调网格_牛客小白月赛134](https://ac.nowcoder.com/acm/contest/137072/F)

若最终中心为 $(x,y)$，格子 $(i,j)$ 保持不变当且仅当  
$$a_{i,j}=\max(|x-i|,|y-j|).$$  
也就是说，若 $a_{i,j}=k$，那么合法中心 $(x,y)$ 必须落在以 $(i,j)$ 为中心、切比雪夫半径为 $k$ 的正方形边界上。

于是对每个格子，把半径 $k$ 的正方形整体加一，再把半径 $k-1$ 的内部正方形减一，就能用二维差分统计每个中心能匹配多少个格子。设最大匹配数为 $mx$，则最少修改次数为 $nm-mx$。

时间复杂度 $\mathcal{O}(nm)$

```c++
void ap() {
	int n, m; cin >> n >> m;
	
	//为了防止边界越界（出现 `i-1` 或 `j-1`），通常**从下标 1 开始**存储数据。
    vector<vector<int>> d(n + 2, vector<int> (m + 2));
    
    auto add = [&](int r1, int c1, int r2, int c2, int val) -> void {
        r1 = max(r1, 1LL), c1 = max(c1, 1LL), r2 = min(r2, n), c2 = min(c2, m);
        if (r1 > r2 || c1 > c2) return ;
        
        d[r1][c1] += val;
        d[r1][c2 + 1] -= val;
        d[r2 + 1][c1] -= val;
        d[r2 + 1][c2 + 1] += val;
        
        return;
    };
    
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= m; j++) {
            int k; cin >> k;
            
            add(i - k, j - k, i + k, j + k, 1);
            if (k > 0) add(i - (k - 1), j - (k - 1), i + (k - 1), j + (k - 1), -1);
        }
    }
    
    /*
    当外层循环执行到第 i 行、第 j 列时，
    d[i-1][j]、d[i][j-1] 和 d[i-1][j-1] 其实已经完成了解密（变成了前缀和值），
    而当前的 d[i][j] 还是纯粹的差分值。
    */
    int mx = -1;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= m; j++) {
            d[i][j] = d[i - 1][j] + d[i][j - 1] - d[i - 1][j - 1] + d[i][j];
            
            mx = max(mx, d[i][j]);
        }
    }
    
    cout << n * m - mx << endl;
    
    
    
	return ;
}
```



## 关联笔记
- [CodeForces](CodeForces.md) — CF 竞赛笔记
- [2026 NC CCPC](2026%20NC%20CCPC.md) — CCPC 真题
