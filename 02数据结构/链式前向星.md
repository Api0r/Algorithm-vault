
```c++
#include <bits/stdc++.h>  
using namespace std;  
#define int long long
#define endl '\n'
const int mod = 1e9 + 7;
const int MOD = 998244353;

// 点的最大数量
const int MAXN = 11;//// 必须加上 const，告诉编译器这是不会改变的常量

// 边的最大数量
// 只有链式前向星方式建图需要这个数量
// 注意如果无向图的最大数量是m条边，数量要准备m*2
// 因为一条无向边要加两条有向边
const int MAXM = 21;

// 链式前向星方式建图
int head[MAXN];//点号 的 头边号
int nxt[MAXM];//边号 的下一个边的 边号
int to[MAXM];//边号 指向的 点号

//点的编号为1...n
int cnt = 1;

// 如果边有权重，那么需要这个数组
int weight[MAXM];

void build(int n) {
    // 链式前向星清空
    cnt = 1;
    for (int i = 1; i < n + 1; i++) {
        head[i] = 0;
    }
}

void addEdge(int u, int v, int w) {
    // u -> v , 边权重是w
    nxt[cnt] = head[u];
    to[cnt] = v;
    weight[cnt] = w;
    head[u] = cnt++;
}

void ap() {
	int edges[][3] = { { 1, 3, 6 }, { 4, 3, 4 }, { 2, 4, 2 }, { 1, 2, 7 }, { 2, 3, 5 }, { 3, 1, 1 } };
    int n = 4;
    // 链式前向星建图
    for (auto & edge : edges) {
        addEdge(edge[0], edge[1], edge[2]);
        //addEdge(edge[1], edge[0], edge[2]);无向图就加这个
    }

    for (int i = 1; i <= n; i++) {
        cout << i << "(邻居、边权) : ";
        
        // 注意这个for循环，链式前向星的方式遍历
        for (int ei = head[i]; ei > 0; ei = nxt[ei]) {
            cout << '(' << to[ei] << ',' << weight[ei] << ')'; 
        }
        cout << endl;    
    }
    
	return ;
}

signed main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int T = 1;
//	cin >> T;
	while (T--) {
		ap();
	} 
    return 0;
}
```

```c++
1(邻居、边权) : (2,7)(3,6)
2(邻居、边权) : (3,5)(4,2)
3(邻居、边权) : (1,1)
4(邻居、边权) : (3,4)
```