```c++
/*
用途：一维树状数组，默认维护单点加、前缀和与区间和。

接口（均为 1-indexed）：modify(p,x)、ask(p)、ask(l,r)、askKth(k)。
update(l,r,x) 是差分用法：区间加、单点查询；不要与区间和语义混用。
askKth(k) 返回最小的 p 使前缀和 >= k；要求各点权非负，非法 k 返回 n + 1。
*/
template <class T>
struct BIT {
    int n = 0;
    vector<T> bit;

    BIT() = default;
    explicit BIT(int n) {
        init(n);
    }

    void init(int _n) {
        n = _n;
        bit.assign(n + 1, T{});
    }

    template <class A>
    void build(const A &a) {
        fill(bit.begin(), bit.end(), T{});
        for (int i = 1; i <= n; ++i) {
            bit[i] += a[i];
            int j = i + (i & -i);
            if (j <= n) bit[j] += bit[i];
        }
    }

    //将位置 x 增加 v
    void modify(int x, const T &v) {
        assert(1 <= x && x <= n);
        for (; x <= n; x += x & -x) bit[x] += v;
    }

    void update(int l, int r, const T &v) {
        if (l > r) return;
        assert(1 <= l && r <= n);
        modify(l, v);
        if (r < n) modify(r + 1, -v);
    }

    //查询前缀和 [1, x]
    T ask(int x) const {
        x = min(x, n);
        T res{};
        for (; x > 0; x -= x & -x) res += bit[x];
        return res;
    }

    T ask(int l, int r) const {
        if (l > r) return T{};
        assert(1 <= l && r <= n);
        return ask(r) - ask(l - 1);
    }

    int askKth(const T &k) const {
        if (!(k > T{})) return 1;
        int pos = 0;
        T pre{};
        int stp = 1;
        while ((stp << 1) <= n) stp <<= 1;
        for (; stp; stp >>= 1) {
            int nxt = pos + stp;
            if (nxt <= n && pre + bit[nxt] < k) {
                pre += bit[nxt];
                pos = nxt;
            }
        }
        return pos + 1;
    }
};
```

```c++
vector<int> tree;

void update(int i, int val) {
    for (; i < tree.size(); i += i & -i) {
        tree[i] += val;
    }
}

int pre(int i) {
    int ans = 0;
    for (; i > 0; i -= i & -i) {
        ans += tree[i];
    }
    return ans;
}

int query(int l, int r) {
    if (r < l) return 0;

    return pre(tree, r) - pre(tree, l - 1);
}
```

```c++
void update(vector<int> & tree, int i, int val) {
    for (; i < tree.size(); i += i & -i) {
        tree[i] += val;
    }
}

int pre(const vector<int> & tree, int i) {
    int ans = 0;
    for (; i > 0; i -= i & -i) {
        ans += tree[i];
    }
    return ans;
}

int query(const vector<int> & tree, int l, int r) {
    if (r < l) return 0;

    return pre(tree, r) - pre(tree, l - 1);
}
```

![](image/Pasted%20image%2020260825222951.png)

这份代码是一个高度工程化的 **一维树状数组（Binary Indexed Tree / Fenwick Tree）** 模板，封装了初始化、线性建树、单点/区间修改、前缀/区间查询以及倍增查找第 $k$ 大等常用功能。

### 1. 核心原理与结构成员

树状数组利用二进制的最低有效位（`lowbit(x) = x & -x`）来维护长度为 `lowbit(x)` 的区间和：

- 每个节点 `bit[x]` 维护原数组在区间 `(x - lowbit(x), x]` 的和。

- 内部使用 `1-based indexing`（下标从 $1$ 到 $n$），因此大小分配为 `n + 1`。

### 2. 各成员函数逐行详解

**初始化与构造**

```c++
void init(int _n) {
    n = _n;
    bit.assign(n + 1, T{});
}
```

- 重设树状数组大小，并用泛型默认值 `T{}`（如 `0`）填充。


**$O(n)$ 线性建树**

```c++
template <class A>
void build(const A &a) {
    fill(bit.begin(), bit.end(), T{});
    for (int i = 1; i <= n; ++i) {
        bit[i] += a[i];
        int j = i + (i & -i);
        if (j <= n) bit[j] += bit[i];
    }
}
```

- **传统建树：** 调用 $n$ 次 `modify`，复杂度为 $O(n \log n)$。

- **线性优化：** 树状数组本质是一棵树，节点 $i$ 的直接父亲即为 $j = i + \text{lowbit}(i)$。自底向上遍历时，将当前节点的和直接累加给直接父亲节点，即可在 $O(n)$ 时间内完成建树。



**单点修改 `modify` 与 前缀和查询 `ask`**

```c++
void modify(int x, const T &v) {
    assert(1 <= x && x <= n);
    for (; x <= n; x += x & -x) bit[x] += v;
}

T ask(int x) const {
    x = min(x, n);
    T res{};
    for (; x > 0; x -= x & -x) res += bit[x];
    return res;
}
```

- `modify(x, v)`：将位置 $x$ 增加 $v$。自底向上沿树链更新所有包含 $x$ 的区间，步进方式为 $x \leftarrow x + \text{lowbit}(x)$，时间复杂度 $O(\log n)$。

- `ask(x)`：查询前缀和 $\sum_{i=1}^x a_i$。自顶向下拆解区间，步进方式为 $x \leftarrow x - \text{lowbit}(x)$，时间复杂度 $O(\log n)$。



**区间操作：差分修改与区间求和**

```c++
void update(int l, int r, const T &v) {
    if (l > r) return;
    assert(1 <= l && r <= n);
    modify(l, v);
    if (r < n) modify(r + 1, -v);
}

T ask(int l, int r) const {
    if (l > r) return T{};
    assert(1 <= l && r <= n);
    return ask(r) - ask(l - 1);
}
```

- `ask(l, r)`：常规树状数组用法，利用前缀和差分计算区间和：$\text{Sum}(l, r) = \text{ask}(r) - \text{ask}(l - 1)$。

- `update(l, r, v)`：**差分数组用法**。在差分语义下，对区间 $[l, r]$ 加上 $v$ 等价于 `d[l] += v` 且 `d[r + 1] -= v`。此时调用 `ask(p)` 得到的将是**单点 $p$ 的实际值**，不可与前缀和语义混用。



**倍增查询第 $k$ 小/大位置 `askKth`**

```c++
int askKth(const T &k) const {
    if (!(k > T{})) return 1;
    int pos = 0;
    T pre{};
    int stp = 1;
    while ((stp << 1) <= n) stp <<= 1;
    for (; stp; stp >>= 1) {
        int nxt = pos + stp;
        if (nxt <= n && pre + bit[nxt] < k) {
            pre += bit[nxt];
            pos = nxt;
        }
    }
    return pos + 1;
}
```

该函数用于在**权值全部非负**（即前缀和单调不降）时，找出最小的下标 $p$，使得 $\sum_{i=1}^p a_i \ge k$。

- **原理：** 若使用“二分 + `ask`”，复杂度为 $O(\log^2 n)$。这里利用二进制提升（倍增）思想，直接在树状数组结构上跳跃：

    1. `stp` 初始化为小于等于 $n$ 的最大 2 的次幂（最高二进制位）。

    2. 尝试从 `pos` 向右跳跃 `stp` 步到达 `nxt = pos + stp`。由于 `pos` 一定是 $stp$ 的倍数，节点 `bit[nxt]` 恰好完整覆盖了区间 `(pos, nxt]` 的和。

    3. 如果累加后仍严格小于 $k$，说明目标位置在更右侧，执行 `pos = nxt` 并累加和；否则不跳。

    4. 最终 `pos` 会停在满足“前缀和 $< k$”的最大位置，因此 `pos + 1` 即为所求的最小下标。

- **复杂度：** 仅需 $O(\log n)$，常数极小。如果整个数组的总和仍小于 $k$，最终会返回 $n + 1$。

