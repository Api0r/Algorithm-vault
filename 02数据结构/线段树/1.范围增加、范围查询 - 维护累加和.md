
```c++
// 线段树模板：区间加法、区间和查询（1-indexed）
// 线段树支持范围增加、范围查询
// 维护累加和
// 测试链接 : https://www.luogu.com.cn/problem/P3372

/*
用途：线段树（区间加法、区间和查询）。
特性：基于懒标记（Lazy Tag）实现，支持 1-indexed 数据与查询。
复杂度：建树 O(n)，区间加 O(log n)，区间查询 O(log n)，空间复杂度 O(n)。

接口（均为 1-indexed）：
  - build(arr)          : 传入 1-indexed 的原始数组以 O(n) 完成建树。
  - add(l, r, v)        : 将区间 [l, r] 内所有元素加上增量 v。
  - query(l, r)         : 查询区间 [l, r] 的累加和。

注意事项：
  - 传入的原始数组 `arr` 大小需至少为 n + 1，有效数据从下标 1 开始。
  - 内部空间分配为 4 倍节点大小，单点修改可使用 add(p, p, v)。
  - 若用于累加和大范围数据，建议模板参数类型使用 long long 防止溢出。
*/
template <typename T = long long>
struct SegTree {
    int n;
    vector<T> sum;
    vector<T> add_tag;

    SegTree(int size) : n(size), sum((size + 1) << 2, 0), add_tag((size + 1) << 2, 0) {}

    // 向上汇总
    void up(int i) {
        sum[i] = sum[i << 1] + sum[i << 1 | 1];
    }

    // 懒标记处理
    void lazy(int i, T v, int len) {
        sum[i] += v * len;
        add_tag[i] += v;
    }

    // 向下传递懒标记
    void down(int i, int ln, int rn) {
        if (add_tag[i] != 0) {
            lazy(i << 1, add_tag[i], ln);
            lazy(i << 1 | 1, add_tag[i], rn);
            add_tag[i] = 0;
        }
    }

    // 建树
    void build(int l, int r, int i, const vector<T>& arr) {
        if (l == r) {
            sum[i] = arr[l];
            return;
        }
        int mid = (l + r) >> 1;
        build(l, mid, i << 1, arr);
        build(mid + 1, r, i << 1 | 1, arr);
        up(i);
        add_tag[i] = 0;
    }

    // 区间加：[jobl, jobr] 增加 jobv
    void add(int jobl, int jobr, T jobv, int l, int r, int i) {
        if (jobl <= l && r <= jobr) {
            lazy(i, jobv, r - l + 1);
            return;
        }
        int mid = (l + r) >> 1;
        down(i, mid - l + 1, r - mid);
        if (jobl <= mid) {
            add(jobl, jobr, jobv, l, mid, i << 1);
        }
        if (jobr > mid) {
            add(jobl, jobr, jobv, mid + 1, r, i << 1 | 1);
        }
        up(i);
    }

    // 区间查询：[jobl, jobr] 的区间和
    T query(int jobl, int jobr, int l, int r, int i) {
        if (jobl <= l && r <= jobr) {
            return sum[i];
        }
        int mid = (l + r) >> 1;
        down(i, mid - l + 1, r - mid);
        T ans = 0;
        if (jobl <= mid) {
            ans += query(jobl, jobr, l, mid, i << 1);
        }
        if (jobr > mid) {
            ans += query(jobl, jobr, mid + 1, r, i << 1 | 1);
        }
        return ans;
    }

    // 便捷对外接口
    void build(const vector<T>& arr) { build(1, n, 1, arr); }
    void add(int l, int r, T v) { add(l, r, v, 1, n, 1); }
    T query(int l, int r) { return query(l, r, 1, n, 1); }
};
```



**测试链接代码：**

```c++
#include <bits/stdc++.h>  
using namespace std;  
#define int long long
#define endl '\n'
const int mod = 1e9 + 7;
const int MOD = 998244353;

// 线段树模板：区间加法、区间和查询（1-indexed）
// 线段树支持范围增加、范围查询
// 维护累加和
// 测试链接 : https://www.luogu.com.cn/problem/P3372
template <typename T = long long>
struct SegTree {
    int n;
    vector<T> sum;
    vector<T> add_tag;

    SegTree(int size) : n(size), sum((size + 1) << 2, 0), add_tag((size + 1) << 2, 0) {}

    // 向上汇总
    void up(int i) {
        sum[i] = sum[i << 1] + sum[i << 1 | 1];
    }

    // 懒标记处理
    void lazy(int i, T v, int len) {
        sum[i] += v * len;
        add_tag[i] += v;
    }

    // 向下传递懒标记
    void down(int i, int ln, int rn) {
        if (add_tag[i] != 0) {
            lazy(i << 1, add_tag[i], ln);
            lazy(i << 1 | 1, add_tag[i], rn);
            add_tag[i] = 0;
        }
    }

    // 建树
    void build(int l, int r, int i, const vector<T>& arr) {
        if (l == r) {
            sum[i] = arr[l];
            return;
        }
        int mid = (l + r) >> 1;
        build(l, mid, i << 1, arr);
        build(mid + 1, r, i << 1 | 1, arr);
        up(i);
        add_tag[i] = 0;
    }

    // 区间加：[jobl, jobr] 增加 jobv
    void add(int jobl, int jobr, T jobv, int l, int r, int i) {
        if (jobl <= l && r <= jobr) {
            lazy(i, jobv, r - l + 1);
            return;
        }
        int mid = (l + r) >> 1;
        down(i, mid - l + 1, r - mid);
        if (jobl <= mid) {
            add(jobl, jobr, jobv, l, mid, i << 1);
        }
        if (jobr > mid) {
            add(jobl, jobr, jobv, mid + 1, r, i << 1 | 1);
        }
        up(i);
    }

    // 区间查询：[jobl, jobr] 的区间和
    T query(int jobl, int jobr, int l, int r, int i) {
        if (jobl <= l && r <= jobr) {
            return sum[i];
        }
        int mid = (l + r) >> 1;
        down(i, mid - l + 1, r - mid);
        T ans = 0;
        if (jobl <= mid) {
            ans += query(jobl, jobr, l, mid, i << 1);
        }
        if (jobr > mid) {
            ans += query(jobl, jobr, mid + 1, r, i << 1 | 1);
        }
        return ans;
    }

    // 便捷对外接口
    void build(const vector<T>& arr) { build(1, n, 1, arr); }
    void add(int l, int r, T v) { add(l, r, v, 1, n, 1); }
    T query(int l, int r) { return query(l, r, 1, n, 1); }
};

void ap() {
    int n, m; cin >> n >> m;
    //注意：这里a数组下标必须从1 - n。因为板子规定下标从1开始
    vector<int> a(n + 1);
    for (int i = 1; i <= n; i++) cin >> a[i];

    SegTree<int> tr(n);
    tr.build(a);
    
    while (m--) {
        int op; cin >> op;
        if (op == 1) {
            int x, y, k; cin >> x >> y >> k;

            tr.add(x, y, k);
        } else {
            int x, y; cin >> x >> y;

            cout << tr.query(x, y) << endl;
        }
    }

    
	return ;
}

signed main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int T = 1;
//	cin >> T;
	while (T--) {
		ap();
	} 
    return 0;
}
```