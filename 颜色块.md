[D-小红的神秘密码解锁_牛客周赛 Round 129](https://ac.nowcoder.com/acm/contest/127264/D)
![](image/Pasted%20image%2020260319203139.png)```
```c++
void solve() {
	string s;
    cin >> s;
    int cnt1 = 0, cnt0 = 0;
    for (int i = 1; i < s.size(); i++) {
        if (s[i - 1] != s[i]) {
            cnt0++;
        } else {
            cnt1++;
        }
    }
    cout << cnt0 * cnt1 + 1 << endl;
//     int n = s.size();
//     if(n == 1) {
//         cout << 1 << endl;
//         return;
//     }

//     int cnt0 = 0;
//     for (int i = 0; i < n - 1; i++) {
//         if(s[i] == s[i + 1]) {
//             cnt0++;
//         }
//     }
//     int cnt1 = (n - 1) - cnt0;
//     int ans = cnt0 * cnt1 + 1;
//     cout << ans << endl;
    return;
}
```

颜色块的数量是 s[i] != s[i + 1] 的个数 + 1

对于翻转的区间，区间内的块数是不变的，变的只会是端点。
- 如果端点原来是相等的，改变之后就不相等，那么另外一个端点就需要原来不相等
 - 需要注意的是，我们可以额外的翻转整个
所以我们只需要计算 s[i] != s[i + 1] 的个数cnt0 和 s[i] == s[i + 1] 的个数cnt1
对于每一个!=, 其余的所有的 == 都可以与组成一个区间，所以cnt0 * cnt1,
注意：我们这里还需要从 == 对于 ！= 来看吗，答案是不需要的，因为两者其实是同时发生的

这个问题可以看成是握手问题，两个人相互握手了，相当于同一次握手


## 关联笔记
- [[区间问题]] — 区间翻转、区间问题
