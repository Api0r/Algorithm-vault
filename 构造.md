# 构造  字符串计数
[F-小红的好串构造_牛客周赛 Round 145](https://ac.nowcoder.com/acm/contest/134981/F)
![[Pasted image 20260525163011.png]]
```c++
void ap() {
	int n, k; cin >> n >> k;
    
    int t = k - (n - 1);
    string s;
    
    for (int i = 0; i < t; i++) s += 'c';
    
    s += "ac";
    
    for (int i = 0; i < n - 2 - t; i++) s += 'b';
    
    cout << s << endl;
	return ;
}
```

思路：
我们看k的范围，k >= n - 1

首先，考虑最小的原则，构造一个abbbb...b ，一共n个字符，n - 1 个b，有n - 1 个好串
符合题目要求的最小范围，说明我们这种构造方法有可能和我们最终想要的 构造方法是**类构造**的

考虑最大，abababab...ab 这种情况是 $n^2$ 级别的，远大于所需的最大，所以不是类构造，否

因此，我们从最小的方法入手 abbb...bb
增加一个b，会有+1的贡献，但是初始时已经满了，所以我们不考虑增加，考虑 删除
删除一个b，会有-1的贡献，我们想要使其慢慢的增加，就需要找到一个+2的贡献与其抵消

这样的话，综合来看，每删一个b，会产生+1 的贡献，最多可以删n - 1个b，即最多增加n - 1个贡献，即最大有2 * (n - 1)的贡献，正好大于题目所需的最大。说明我们更接近答案了

babbb..b 这样的构造，删除一个b，会有 -2 的贡献，那么我们如果加一个b呢? 会有 +2 的贡献

所以我们想办法构造这样的情况

**acbbb...b**
这种构造，初始时，有n - 2个b，加上ac 的贡献，一共有n - 1 的好串，满足题目的最小！ 类构造?
继续看
删除一个b，我们只需要在左边加上一个c，总体就会有 +1 的贡献，符合

最大：删除n -2 个b，最多增加n - 2的贡献, 加上初始的n - 1，最大有2 * n - 3，正好大于题目的2 * (n - 2)， 符合

到这里，构造就出来了，我们再看代码如何表达
答案需要k个贡献，初始时acbbb..b 有n - 1 的贡献，那么我们需要删除 k - (n - 1) 个b，同时增加k - (n - 1) 个c，到这里其实就结束了，因为我们只需要在后面补齐b

具体的，令t = k - (n - 1);
先加 t 个c， 再加ac , 最后补齐b，即 n - t - 2个b
```c++
for (int i = 0; i < t; i++) s += 'c';

s += "ac";

for (int i = 0; i < n - 2 - t; i++) s += 'b';
```


# 贪心 构造 树
[E-战术通信：终端基站的构造_牛客小白月赛133](https://ac.nowcoder.com/acm/contest/135365/E)
![[Pasted image 20260530155507.png]]
```c++
void ap() {
	int n, l; cin >> n >> l;
    vector<array<int, 2>> d(n + 1);
    for (int i = 1; i <= n; i++) {
        cin >> d[i][0];
        d[i][1] = i;
    }
    
    sort(d.begin() + 1, d.end());
    if (d[l + 1][0] == 1 || l == 1) {
        cout << -1 << endl;
        return;
    }
    
//     reverse(d.begin() + 1 + l, d.end());
    
    vector<array<int, 2>> ans;
    
    int idx = l + 1;
    for (int i = idx; i <= n; i++) {
        if (i < n) {
            ans.push_back({d[i][1], d[i + 1][1]});
            d[i][0]--, d[i + 1][0]--;
        } else {
            ans.push_back({d[n][1], d[l][1]});
            l--, d[n][0]--;
        }
    }
    
    for (int i = idx; i <= n; i++) {
        while (l > 0 && d[i][0] > 0) {
            ans.push_back({d[i][1], d[l][1]});
            l--, d[i][0]--;
        }
    }
    
    if (l != 0) {
        cout << -1 << endl;
        return;
    }
    
    for (auto & [x, y] : ans) {
        cout << x << " " << y << endl;
    }
    
    
    
	return ;
}
```

题意：
构造一棵n个节点，n - 1条边的树
要求有L个叶子节点，并且每个节点i的度 最多 为 di

思路：
- 首先特判L= 1时无解(n >= 2, 最少都有2个叶子节点)
- 其次，如果度为1的个数大于L，无解

贪心的，我们将不是叶子节点的 n - L 个节点单独拿出来，先连成一条链，此时除了两端都是非叶子节点
再把剩下的L个叶子节点 接上去，优先接两个端点

如果L > 0 && di0 > 0那么就可以接，可以接就先让这个节点接满
最后再判断是否全部接完了

细节：
1. 为什么一定可以接到两端呢？
- 因为我们保证了剩下的n - L 个节点的度都 >= 2，所以显然可以连成一条链，那么端点 用的度为1，至少还可以接一条



# Dijkstra + 构造

[E-小橙分配边权_牛客小白月赛134](https://ac.nowcoder.com/acm/contest/137072/E)

**知识点**：图论、最短路性质、构造

若存在源点 $s$，则必须有 $a_s=0$。对任意边 $(u,v)$，由于最短路距离分别为 $a_u,a_v$，边权至少需要满足 $w(u,v)\ge |a_u-a_v|$。因此若直接令每条边权为 $|a_u-a_v|$，任意路径长度都不会小于终点的 $a$ 值。

接下来只需要保证每个点都能从某个 $a_s=0$ 的点出发，沿着 $a$ 值不下降的路径到达。这样路径长度会发生望远镜求和，恰好为 $a_i$。

若不存在这样的零点或存在点不可达，则无解；否则输出每条边的 $|a_u-a_v|$ 即可。

时间复杂度 $\mathcal{O}(n+m)$​

```c++
void ap() {
	int n, m; cin >> n >> m;
    vector<int> a(n);
    for (int & i : a) cin >> i;
    
    vector<vector<pair<int, int>>> g(n);
    vector<int> w(m);
    for (int i = 0; i < m; i++) {
        int u, v; cin >> u >> v;
        u--, v--;
        
        w[i] = abs(a[u] - a[v]);
        g[u].push_back({v, w[i]});
        g[v].push_back({u, w[i]});
    }
    
    int s = -1;
    for (int i = 0; i < n; i++) {
        if (a[i] == 0) s = i;
    }
    
    if (s == -1) {
        cout << -1 << endl;
        return;
    }
    
    vector<int> dis(n, 2e18);
    // 堆中保存 (起点到节点 x 的最短路长度，节点 x)
    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<>> pq;
    dis[s] = 0; // 起点到自己的距离是 0
    pq.emplace(0, s);

    while (!pq.empty()) {
        auto [dis_x, x] = pq.top();
        pq.pop();
        if (dis_x > dis[x]) { // x 之前出堆过
            continue;
        }
        for (auto& [y, wt] : g[x]) {
            auto new_dis_y = dis_x + wt;
            if (new_dis_y < dis[y]) {
                dis[y] = new_dis_y; // 更新 x 的邻居的最短路
                // 懒更新堆：只插入数据，不更新堆中数据
                // 相同节点可能有多个不同的 new_dis_y，除了最小的 new_dis_y，其余值都会触发上面的 continue
                pq.emplace(new_dis_y, y);
            }
        }
    }

    for (int i = 0; i < n; i++) {
        if (dis[i] != a[i]) {
            cout << -1 << endl;
            return;
        }
    }
    
    for (int i = 0; i < m; i++) {
        cout << w[i] << endl;
    }
    
	return ;
}
```



## 关联笔记
- [[图论]] — Dijkstra + 构造
- [[树]] — 树构造
