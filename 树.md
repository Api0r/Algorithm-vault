# 图论 生成树 并查集
[E-小红的染色生成树_牛客周赛 Round 145](https://ac.nowcoder.com/acm/contest/134981/E)
![](image/Pasted%20image%2020260525154436.png)
```c++
void ap() {
	int n, m; cin >> n >> m;
    
    vector<array<int, 3>> e(m);
    vector<int> idx(3, -1);
    for (int i = 0; i < m; i++) {
        cin >> e[i][0] >> e[i][1] >> e[i][2];
        idx[e[i][2]] = i;//最后一个 这个 颜色的下标
    }
    
    if (n < 3) {
        cout << -1 << endl; return;
    }
    
    
    for (int x = 0; x < 3; x++) {
        for (int y = x + 1; y < 3; y++) {
            if (idx[x] == -1 || idx[y] == -1) continue;
            
            fa.resize(n + 1);
            for (int i = 0; i < n + 1; i++) {
                fa[i] = i;
            }
            
            vector<pair<int, int>> ans;
            
            int i = idx[x], j = idx[y];//拿到x，y颜色的边的下标
            //强行加边。先把颜色为x，y 的边直接加进去
            if (merge(e[i][0], e[i][1])) ans.push_back({e[i][0], e[i][1]});
            if (merge(e[j][0], e[j][1])) ans.push_back({e[j][0], e[j][1]});
            
            for (auto [u, v, w] : e) {
                if (w != x && w != y) continue;
                
                if (merge(u, v)) ans.push_back({u, v});
            }
            
            if (ans.size() == n - 1) {
                for (auto & [u, v] : ans) cout << u << " " << v << endl;
                return ;
            }
        }
    }
    
    cout << -1 << endl;
    
	return ;
}
```
![](image/Pasted%20image%2020260525155155.png)
**图论定理**：在任意连通图 $G$ 中，如果你先任意选择了几条不冲突的边组成了一个森林 $F$。只要 $G$ 本身是连通的，你就一定可以通过在 $G$ 中继续寻找其他的边加入 $F$，将其安全地扩展为一棵完整的生成树。

所以我们枚举两种颜色的情况，只有(0, 1) (0, 2) (1, 2) 三种
每次枚举的时候，我们强行加入x，y两种颜色的边。由上面的定理，我们只需要维护加入的边不冲突，就可以安全的将其扩展为完整的生成树

维护边不冲突，可以利用并查集的merge判断及实现，其可以维护不出现环

# 树论

[F-小红的二叉树_牛客周赛 Round 148](https://ac.nowcoder.com/acm/contest/136707/F)
![](image/Pasted%20image%2020260615190224.png)

```c++
int n;
int lc(int x) {
    return x << 1;
}

int rc(int x) {
    return x << 1 | 1;
}

int calc1(int u, int d) {//计算以 `u` 为根的子树中，与 `u` 的距离恰好为 `d` 的节点个数。
    int l = u, r = u;
    for (int i = 1; i <= d; i++) {
        l = lc(l), r = rc(r);
        if (l > n) return 0;
    }
    return min(r, n) - l + 1;
}

int calc2(int u, int d) {//不在 `u` 的子树中，但与 `u` 的距离恰好为 `d` 的节点个数
    int ans = 0;
    for (int i = 1; i <= d; i++) {
        if (u == 1) break;
        
        if (i == d) {
            ans++;
        } else {
            ans += calc1(u ^ 1, d - i - 1);
        }
        
        u /= 2;
    }
    return ans;
}

void ap() {
	int x; cin >> n >> x;
    
    int ans = 0;
    for (int i = 1; i <= 60; i++) {
        int c1 = calc1(lc(x), i - 1);
        int c2 = calc1(rc(x), i - 1);
        int c3 = calc2(x, i);
        
        ans += c1 * c2 + c1 * c3 + c2 * c3;
    }
    cout << ans + 1 << endl;
	return ;
}
```



## 关联笔记
- [[图论]] — 图搜索与树
- [[二叉树遍历]] — 二叉树遍历
- [[Morris 中序遍历]] — Morris 遍历
- [[树论]] — 树论专题
