https://leetcode.cn/problems/car-pooling/solutions/2550264/suan-fa-xiao-ke-tang-chai-fen-shu-zu-fu-9d4ra

# 时间段重合 | 差分
[L2-3 胖达的山头 - 热身赛10](https://pintia.cn/problem-sets/2044606403164971008/exam/problems/type/7?problemSetProblemId=2044606403450245122)
![](image/Pasted%20image%2020260416204414.png)

```c++
#include<bits/stdc++.h>
using namespace std;

int ap_t(string& s) {
    int ans = stoi(s.substr(0,2)) * 60 * 60 + stoi(s.substr(3, 2)) * 60 + stoi(s.substr(6, 2));
    return ans;
} 

signed main () {
    int n; cin >> n;
    cin.ignore();
    //5184000

    vector<int> d(5184001);
    for (int i = 0; i < n; i++) {
        string a, b; cin >> a >> b;
        int x = ap_t(a), y = ap_t(b);

        d[x]++, d[y + 1]--;
    }

    int ans = 0;
    int sum = 0;
    for (int i = 0; i < d.size(); i++) {
        sum += d[i];
        ans = max(ans, sum);
    }
    cout << ans;
    cout << endl;

    return 0;
}
```

题意：需要把熊猫分开，使得每个时间段都不重合。求最少需要的山头。
我们可以把时间段看成是区间，答案其实就是 **重叠区间上 重叠个数的最大值**

这个可以巧妙地用差分解决。每个时间段端点 单次操作。最后求一遍前缀和恢复，一次遍历得最大值

## 关联笔记
- [前缀和](前缀和.md) — 差分是前缀和的逆运算，二者配合使用
- [区间问题](区间问题.md) — 区间重叠、区间操作问题
- [区间问题的类型及解法](区间问题/区间问题的类型及解法.md) — 区间问题方法论