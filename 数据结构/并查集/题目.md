
# 一、普通并查集的巧妙做法

[F-小橙的异或和_牛客小白月赛129](https://ac.nowcoder.com/acm/contest/128675/F)
![](../../image/Pasted%20image%2020260625112335.png)
每个数最多被修改32次就会变成0，变成0之后无论怎操作都是0
也就是说，最多只需要修改n * 32 次，就无需再修改

- 对于需要修改的区间中的元素，如果是0，那么跳过
- 我们可以用set集合存储非零的下标
- 通过set内lower_bound找到第一个 >= l + 1的下标，然后迭代遍历
- set的增删时间复杂度为logn

- 可以用并查集维护，作为跳板，如果当前数变为了0，那么将其fa指向下一个数的代表节点
- 那么每次我们只需要find(i + 1)，找到下一个非零的点。
- 因为中间所有的0都被合并了，都指向下一个非零的点

```c++
vector<int> fa;

int find (int i) {
    if (fa[i] != i) {
        fa[i] = find(fa[i]);
    }
    return fa[i];
}

bool merge(int i, int j) {
    int x = find(i), y = find(j);
    if (x == y) return false;
    
    fa[x] = y;
    return true;
}

void ap() {
	int n, q; cin >> n >> q;
    vector<int> a(n);
    int x = 0;
    for (int & i : a) cin >> i, x ^= i;
    
    fa.resize(n + 5);//注意这里需要开大一位，因为后面会用到i = find(i + 1);
    for (int i = 0; i < n + 5; i++) {
        fa[i] = i;
    }
    
    while (q--) {
        int opt; cin >> opt;
        if (opt == 1) {
            int l, r; cin >> l >> r;
            l--, r--;
            
            int i = find(l + 1);
            while (i <= r) {
                int t = a[i] / (i - l + 1);
                x = x ^ a[i] ^ t;
                
                a[i] = t;
                
                if (a[i] == 0) {
                    fa[i] = find(i + 1);
                }
                
                i = find(i + 1);
            }
        } else {
            cout << x << endl;
        }
    }
    
    
    
	return ;
}
```

- set做法
```c++
void ap() {
    int n, q; 
    cin >> n >> q;
    vector<int> a(n);
    int x = 0;
    
    set<int> active_idx; // 存储所有当前值不为 0 的下标

    for (int i = 0; i < n; i++) {
        cin >> a[i];
        x ^= a[i]; // 维护初始全局异或和
        if (a[i] > 0) {
            active_idx.insert(i); // 只有非 0 元素的下标才放进去
        }
    }
    
    while (q--) {
        int opt; cin >> opt;
        if (opt == 1) {
            int l, r; cin >> l >> r;
            l--, r--; // 转换为 0-indexed
            
            // 题意：当 i = l 时除数是 1，值不变。因此我们直接找 >= l + 1 的第一个非零位置
            auto it = active_idx.lower_bound(l + 1);
            
            // 遍历所有在 [l+1, r] 闭区间内的有效非零下标
            while (it != active_idx.end() && *it <= r) {
                int i = *it;
                int t = a[i] / (i - l + 1);
                
                x = x ^ a[i] ^ t; // 更新异或和
                a[i] = t;
                
                if (a[i] == 0) {
                    // 如果变成 0 了，需要从 set 中删掉
                    // erase(it++) 是标准写法：删除当前迭代器，并让 it 顺势指向下一个
                    active_idx.erase(it++); 
                } else {
                    // 如果没变成 0，继续往后看
                    it++; 
                }
            }
        } else {
            cout << x << endl;
        }
    }
    return;
}
```

# 二、

[C-Fish Eating_2026牛客暑期多校训练营1](https://ac.nowcoder.com/acm/contest/133876/C)

**单调递增的序列**

```c++
vector<int> fa, sz, req;//从节点 i 出发，想要吃掉它的父亲节点（以及父亲背后更广阔的世界），
//i 节点所代表的连通块在相遇前，其总大小需要的“最低门槛”。
vector<int> a;
int find(int i) {
//     if (fa[i] != i) {
//         fa[i] = find(fa[i]);
//         req[i] = max(req[i], req[fa[i]]);
//     }错
    if (fa[i] == i) return fa[i];
    
    int root = find(fa[i]);
    req[i] = max(req[i], req[fa[i]]);
    fa[i] = root;
    
    return fa[i];
}

bool merge(int i, int j) {// j -> i
    int x = find(i), y = find(j);
    if (x == y) return false;
    
    fa[y] = x;
    sz[x] += sz[y];
    
    req[y] = max(0LL, a[x] - sz[y] + 1);
    return true;
}
int dx[4] = {0, 0, 1, -1}, dy[4] = {1, -1, 0, 0};

void ap() {
	int n, m, q; cin >> n >> m >> q;

    fa.assign(n * m + 5, 0);
    sz.assign(n * m + 5, 1);
    req.assign(n * m + 5, 0);
    a.assign(n * m + 5, -1);
    for (int i = 0; i < n * m + 5; i++) {
        fa[i] = i;
    }
    
    int l = 0;

    while (q--) {
        int op; cin >> op;
        int x, y; cin >> x >> y;
        x = (x ^ l) - 1, y = (y ^ l) - 1;
        
        if (op == 1) {
            int v; cin >> v;
            a[x * m + y] = v;
            
            for (int i = 0; i < 4; i++) {
                int nx = x + dx[i], ny = y + dy[i];
                if (nx < 0 || nx >= n || ny < 0 || ny >= m) continue;
                if (a[nx * m + ny] == -1) continue;
                
                merge(x * m + y, nx * m + ny);//x,y一定是最大的，挂到大的上
            }
            
            l = sz[find(x * m + y)] - 1;
            cout << l << endl;
            
        } else {
            find(x * m + y);
            l = max(0LL, req[x * m + y] - a[x * m + y]);
            
            cout << l << endl;
            
        }
    }
    
	return ;
}
```

细节
```c++
int find(int i) {
    if (fa[i] != i) {
         fa[i] = find(fa[i]);
         req[i] = max(req[i], req[fa[i]]);
    }
    return fa[i];
}
```
这个代码中，fa[ i ] 会不断更新，直到结束，最终fa[i] 指向的是最后的根节点

但是我们需要的是根据上一个节点来更新req数组

所以需要这样写
```c++
int find(int i) {
    if (fa[i] == i) return fa[i];
    
    int root = find(fa[i]);//这里递归完了，返回的是最终的节点
    req[i] = max(req[i], req[fa[i]]);//但是这里的fa[i]仍然是i的上一个节点
    fa[i] = root;//最后在修改fa[i] 为 最终的根节点
    
    return fa[i];
}
```
每次递归，先不直接将fa[i] 连起来，而是先将最终的根节点存起来，再利用上一个节点的信息去更新req，最后在更新fa[i]

