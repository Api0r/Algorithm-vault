
邻接矩阵
`graph` 是 **邻接矩阵**：`graph[i][j] = 1` 表示 i 和 j 相连

邻接表
```c++
vector<vector<int>> g(n);
for (auto& e : edges) {
	int x = e[0], y = e[1];
	g[x].push_back(y);
	g[y].push_back(x); // 无向图
}

```


连通分量的定义为：
~~~c++
设集合 V 为无向图中节点的一个子集，集合 E 为无向图中所有两个端点都在 V 中的边。设图 S=(V,E)，那么 S 就称为无向图的一个「诱导子图」（或者叫「导出子图」）。「连通分量」是极大的「诱导子图」，这里的「极大」表现在：

- V 中的任意两个节点仅通过 E 就可以相互到达；

- 不存在一个不属于 V 的节点 x，使得 x 与 V 中的某个节点直接相连。

我们可以通过节点集合 V 唯一地描述一个连通分量：例如在题目给出的样例 1 中，有两个连通分量 {0,1,2} 和 {3}；样例 2 中，有三个连通分量 {0,1,2,3}，{4} 和 {5}。
~~~

如果在子图中任意两个顶点之间都存在路径，并且子图中没有任何一个顶点与子图外部的顶点共享边，则称其为 **连通分量**

如果连通分量中每对节点之间都存在一条边，则称其为 **完全连通分量**


# 搜索 | 回溯
[797. 所有可能的路径 - 力扣（LeetCode）](https://leetcode.cn/problems/all-paths-from-source-to-target/)
```c++
	vector<vector<int>> ans;
    vector<int> path;

    void dfs(vector<vector<int>> & g, int x, int end) {
        if (x == end) {
            ans.push_back(path);
        }
        for (auto& y : g[x]) {
            path.push_back(y);
            dfs(g, y, end);
            path.pop_back();//路径销毁
        }
    }

    vector<vector<int>> allPathsSourceTarget(vector<vector<int>>& graph) {
        path.push_back(0);
        dfs(graph, 0, graph.size() - 1);
        return ans;
    }
```

# DFS 三色标记法
[207. 课程表 - 力扣（LeetCode）](https://leetcode.cn/problems/course-schedule/description/)
[207. 课程表 - 力扣（LeetCode）](https://leetcode.cn/problems/course-schedule/solutions/2992884/san-se-biao-ji-fa-pythonjavacgojsrust-by-pll7/)
**题意**
给你一个有向图，判断图中是否有环。

**核心思路**
如果在递归过程中，发现下一个节点在递归栈中（正在访问中），则找到了环。
![[Pasted image 20260423145704.png]]

例如 1→2→3→4→2，走到 4 的时候，发现下一个节点 2 在递归栈中（正在访问中），那么就找到了环。

注：我们说节点 x「正在访问中」，是说我们正在递归处理节点 x 以及它的后续节点，dfs(x) 尚未结束。

**具体思路**
对于每个节点 x，都定义三种颜色值（状态值）：

0：节点 x 尚未被访问到。
1：节点 x 正在访问中，dfs(x) 尚未结束。
2：节点 x 已经完全访问完毕。注意这还说明从 x 出发无法找到环。所以当我们遇到状态值为 2 的节点 x 时，无需递归 x。

**⚠误区**：不能只用两种状态表示节点「没有访问过」和「访问过」。例如上图，我们先 dfs(0)，再 dfs(1)，此时 1 的邻居 0 已经访问过，但这并不能表示此时就找到了环。

**算法流程：
![[Pasted image 20260423145809.png]]

```c++
bool canFinish(int n, vector<vector<int>>& prerequisites) {
        vector<vector<int>> g(n);
        for (auto & e : prerequisites) {
            g[e[1]].push_back(e[0]);
        }

        vector<int> col(n);
        auto dfs = [&](this auto&& dfs, int x) -> bool {//是否有环？
            col[x] = 1;
            for (int y : g[x]) {
                if (col[y] == 1 || col[y] == 0 && dfs(y)) {
                    return true;
                }
            }
            col[x] = 2;
            return false;
        };

        for (int i = 0; i < n; i++) {
            if (col[i] == 0 && dfs(i)) {// 有环， 那么不可能
                return false;
            }
        }
        return true;
    }
```

# 两个数组模拟队列
[3243. 新增道路查询后的最短距离 I - 力扣（LeetCode）](https://leetcode.cn/problems/shortest-distance-after-road-addition-queries-i/description/)


# 拓扑排序

[210. 课程表 II - 力扣（LeetCode）](https://leetcode.cn/problems/course-schedule-ii/description/)
## BFS | 入度

**核心是维持一个入度为 0 的顶点的集合．**

```c++
vector<int> findOrder(int n, vector<vector<int>>& prerequisites) {//核心是维持一个入度为 0 的顶点的集合．
	vector<vector<int>> g(n);
	vector<int> in(n);//入度 
	for (const auto & e : prerequisites) {
		int x = e[0], y = e[1];
		g[y].push_back(x);
		in[x]++;
	}

	queue<int> q;
	for (int i = 0; i < n; i++) {
		if (in[i] == 0) {
			q.push(i);
		}
	}

	vector<int> ans;
	while (!q.empty()) {
		int x = q.front();
		q.pop();
		ans.push_back(x);

		for (int y : g[x]) {
			in[y]--;
			if (in[y] == 0) {
				q.push(y);
			}
		}
	}

	if (ans.size() < n) {//如果存在环，那么永远都删不了环里边任意一个节点的前驱！其入读永远 >= 1
		return {};
	}
	return ans;
}
```

### 答疑 BFS 有环就不会全部遍历
假设有环 a -> b -> c -> a
如果我们通过其他的路径走到了a点，此时a的入度由于c节点的存在 ，一定 >= 1
我们想使环中的元素入队，就需要使其入度为0，
- 那么就要删除a 的前驱节点c
- 使c的入度为0，那么需要删除c的前驱节点b
- 同理，需要删除a
- 需要删除c...
问题回到了原点，说明我们无法使其入度为0，所以环中的元素一个都不会入队
不论是一开始，亦或是搜索了一些点，都是如此


## DFS | 三色标记法

维护出度为0，最后reverse 答案数组

逆向思维
当 节点x 的所有相邻节点都已完成时，我们将x放到ans中，并标记col[x] = 2为已完成

即dfs每一个元素，如果没有遍历到，那么dfs，遍历的过程中发现其子节点正在 搜索中，那么有环
否则继续，直到这个节点的所有子相邻节点都搜索完成，那么相当于其出度为0，放到ans中
最终reverse

```c++
vector<int> findOrder(int n, vector<vector<int>>& prerequisites) {
	vector<vector<int>> g(n);
	for (const auto & e : prerequisites) {
		int x = e[0], y = e[1];
		g[y].push_back(x);
	}

	vector<int> ans;
	vector<int8_t> col(n);
	bool ok = true;// 没有环是ok的， 有环就不ok

	auto dfs = [&](this auto&& dfs, int x) -> void {
		col[x] = 1;
		for (int y : g[x]) {
			if (col[y] == 0 && ok) {
				dfs(y);
				if (!ok) return;
			} else if (col[y] == 1) {
				ok = false;
				return;
			}
		}
		col[x] = 2;
		ans.push_back(x);
	};

	for (int i = 0; i < n && ok; i++) {
		if (!col[i]) {
			dfs(i);
		}
	}
	if (!ok) {
		return {};
	}

	reverse(ans.begin(), ans.end());
	return ans;
}
```

# tarjan
求割点 | 桥
[P3388 【模板】割点（割顶） - 洛谷](https://www.luogu.com.cn/problem/solution/P3388)


# 连通图中的任意森林都可以扩展成生成树

**图论定理**：在任意连通图 $G$ 中，如果你先任意选择了几条不冲突的边组成了一个森林 $F$。只要 $G$ 本身是连通的，你就一定可以通过在 $G$ 中继续寻找其他的边加入 $F$，将其安全地扩展为一棵完整的生成树。

# 分层图
## 最多可以更改一个权值的单源最短路
[K-双城之战_第七届武汉纺织大学ACM程序设计竞赛(同步赛）](https://ac.nowcoder.com/acm/contest/136171/K)
```c++
void ap() {
    int n, m, d; 
    cin >> n >> m >> d;
    vector<vector<pair<int, int>>> g(n);
    for (int i = 0; i < m; i++) {
        int u, v, w; cin >> u >> v >> w;
        u--, v--;
        
        g[u].push_back({v, w});
        g[v].push_back({u, w});
    }
    
    vector<vector<int>> dis(n, vector<int>(2, LLONG_MAX));
    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<>> pq;
    
    dis[0][0] = 0; 
    pq.emplace(0, 0);

    while (!pq.empty()) {
        auto [dis_state, state] = pq.top();
        pq.pop();
        
        int x = state / 2;
        int used = state % 2;

        if (dis_state > dis[x][used]) { 
            continue;
        }
        
        for (auto& [y, wt] : g[x]) {
            auto new_dis_y1 = dis_state + wt;
            if (new_dis_y1 < dis[y][used]) {
                dis[y][used] = new_dis_y1;
                pq.emplace(new_dis_y1, y * 2 + used);
            }
            if (used == 0) {
                auto new_dis_y2 = dis_state + (wt % d);
                if (new_dis_y2 < dis[y][1]) {
                    dis[y][1] = new_dis_y2;
                    pq.emplace(new_dis_y2, y * 2 + 1);
                }
            }
        }
    }
    
    int ans = min(dis[n - 1][0], dis[n - 1][1]);
    if (ans == LLONG_MAX) {
        cout << -1 << endl;
    } else {
        cout << ans << endl;
    }
    
    return ;
}
```

## 关联笔记
- [[BFS]] — BFS 搜索与最短路
- [[并查集]] — 连通分量 / DSU 维护连通性
- [[树]] — 生成树、树形结构
- [[构造]] — Dijkstra 构造与图构造