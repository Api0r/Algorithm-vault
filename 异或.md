# 异或操作一个0，1字符串A，是否可以变为给定的01字符B

[D-小苯的01合并_牛客周赛 Round 125](https://ac.nowcoder.com/acm/contest/126319/D)
![[Pasted image 20260319221957.png]]·要把a变为b，其必要条件是A0 == B0,即如果不满足这个，那么一定不会有A == B
贪心的想，我们先使A0 == B0，可以用双指针，如果相等了，那么两个指针一起移，如果没有，那么让A 和右边的数进行异或操作，直到相等。完成之后，贪心的 我们之后就不在异或左边已经完成的数了。如果我们动了左边已经完成的数的话，那么我们又需要满足A0 == B0。
```c++
void solve() {
	int n, m; cin >> n >> m;
    string a; cin >> a;
    string b; cin >> b;
    
    int i = 0, j = 0;
    while (i < n && j < m) {
        if (a[i] != b[j]) {
            if (i == n - 1) {
                cout << "NO" << endl;
                return;
            }
            i++;
            a[i] = (a[i - 1] - '0') ^ (a[i] - '0') + '0';
        } else {
            i++;
            j++;
        }
    }
    
    int rem = 0;
    for (int k = i; k < n; k++) {
        rem ^= (a[k] - '0');
    }
    if (rem == 0 && j == m) {
        cout << "YES" << endl;
    } else {
        cout << "NO" << endl;

    }
    
    
	return ;
}
```
```c++
void solve() {
	int n, m; cin >> n >> m;
    string a; cin >> a;
    string b; cin >> b;
    
    queue<int> q1, q2;
    for (auto& c : a) {
        q1.emplace(c - '0');
    }
    for (auto& c : b) {
        q2.emplace(c - '0');
    }
    while (!q1.empty() && !q2.empty()) {
        if (q1.front() == q2.front()) {
            q1.pop();
            q2.pop();
        } else {
            int x = q1.front();
            q1.pop();
            q1.front() ^= x;
        }
    }
    
    int rem = 0;
    while (!q1.empty()) {
        int x = q1.front();
        q1.pop();
        rem ^= x;
    }
    if (q2.empty() && rem == 0) {
        cout << "YES" << endl;
    } else {
        cout << "NO" << endl;
    }
    return;
}
```

==如果 A 的异或和不等于 B 的异或和，A 肯定不能变成 B

 **证明异或和不变**：

假设原字符串为：`... x y ...`，总异或和为 S

- 操作前：`S = (x ⊕ y) ⊕ (其他部分)`
    
- 操作后：新字符 `z = x ⊕ y`，字符串变为：`... z ...`
    
- 操作后总异或和：`S' = z ⊕ (其他部分) = (x ⊕ y) ⊕ (其他部分) = S`
    

**结论**：无论进行多少次操作，整个字符串所有字符的异或和**永远不变**！

可以把每次操作想象成一个"压缩"过程：

- 两个数字 `x` 和 `y` 被压缩成一个新数字 `x⊕y`
    
- 就像把两瓶水倒在一起，水的总量不变
    
- 异或和就是这个"总量"，无论怎么合并，总量守恒

**异或和相等是必要条件，但不是充分条件**：

- ✅ **必要条件**：如果异或和不相等，肯定不能变成B
    
- ❌ **不是充分条件**：异或和相等也不一定能变成B


# 4.6 cf
[Problem - E - Codeforces](https://codeforces.com/contest/2218/problem/E)
这题的关键就是找规律，猜结论
![[Pasted image 20260406214056.png]]
```c++
void solve() {
	int n; cin >> n;
    vector<int> a(n); for (int i = 0; i < n; i++) cin >> a[i];
    int ans = 0;
    for (int i = 0; i < n; i++) {
        for (int j = i + 1; j < n; j++) {
            ans = max(ans, a[i] ^ a[j]);
        }
    }
    cout << ans << endl;
    


	return ;
}
```


# 若干个 **总和 ≤ s** 的正整数，它们的异或结果 **一定也 ≤ s**
[Problem - G - Codeforces](https://codeforces.com/gym/686522/problem/G)

设：

- 有一堆数：`a₁, a₂, ..., aₖ`
- 它们的**总和**：`sum = a₁ + a₂ + ... + aₖ ≤ s`
- 它们的**异或和**：`xor_sum = a₁ ^ a₂ ^ ... ^ aₖ`

### 关键公式（二进制必背性质）

a+b=(a⊕b)+2×(a&b)

推广到任意多个数：

sum=xor_sum+2×C

其中 **C ≥ 0**（是二进制进位的总和，永远非负）

所以：

非负数

### 直接推出：

xor_sum≤sum

### 而 sum ≤ s

所以：

xor_sum≤s

- 异或 = **不进位加法**
- 加法 = **异或 + 两倍进位**
- 不进位加法的结果 **永远 ≤ 正常加法**

## 关联笔记
- [[位运算]] — 位运算合集
- [[数学]] — a+b = (a⊕b) + 2×(a&b) 公式及其推广
- [[前缀和]] — 前缀异或和
- [[位运算/前缀异或和]] — 前缀异或和专题