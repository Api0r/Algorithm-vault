- **最小深度** 是从根节点到最近叶子节点的最短路径上的节点数量
- **最大深度** 是指从根节点到最远叶子节点的最长路径上的节点数。
- **直径** 是指树中任意两个节点之间最长路径的 **长度**
- 两节点之间路径的 **长度** 由它们之间边数表示
# 给定后序 + 中序 还原二叉树


[L2-006 树的遍历 - 团体程序设计天梯赛-练习集](https://pintia.cn/problem-sets/994805046380707840/exam/problems/type/7?problemSetProblemId=994805069361299456&page=1)
- **中序遍历**：左子树 → 根 → 右子树
- **后序遍历**：左子树 → 右子树 → 根
- **层序遍历**：从上到下、从左到右一层一层输出（用**队列**实现）


## 从 后序 + 中序 还原二叉树的**核心规律**

1. **后序遍历的最后一个元素，一定是整棵树的根节点**
2. 在**中序遍历**里找到这个根：
    
    - 根左边 = **左子树**所有节点
    - 根右边 = **右子树**所有节点
    
3. 用**递归**重复这个过程：
    
    - 找左子树的根
    - 找右子树的根
    - 直到所有节点都建好

**后序**：`2 3 1 5 7 6 ④`
**中序**：`1 2 3 ④ 5 6 7`
![](image/Pasted%20image%2020260413192043.png)
## 第一步：整棵树的根

后序：`2 3 1 5 7 6 ④`

最后一个是 **4**

→ **整棵树根是 4**

看**中序**：

`1 2 3 ④ 5 6 7`

- 4 左边：`1 2 3` → **左子树**
- 4 右边：`5 6 7` → **右子树**

现在树长这样：

plaintext

```
        4
      /   \
  [1,2,3] [5,6,7]
```

---

## 第二步：建左子树（1,2,3）

左子树有 3 个节点。

在后序里，前 3 个就是左子树的后序：`2 3 ①`

后序最后一个：**1**

→ **左子树根是 1**

看中序：

`① 2 3 | 4 | ...`

- 1 左边：无
- 1 右边：`2 3` → 1 的右子树

现在树：

plaintext

```
         4
       /   \
      1    [5,6,7]
       \
       [2,3]
```

---

## 第三步：建 1 的右子树（2,3）

这部分在后序里是：`2 ③`

最后一个是 **3**

→ 根是 3

看中序：

`1 | ② ③ | ...`

- 3 左边：2
- 3 右边：无

树更新：

plaintext

```
         4
       /   \
      1     6
       \   / \
        3 5   7
       /
      2
```

---

## 第四步：建右子树（5,6,7）

右子树共 3 个节点，在后序里是：`5 7 ⑥`

最后一个是 **6**

→ 右子树根是 6

看中序：

`... | 4 | ⑤ ⑥ ⑦`

- 6 左边：5
- 6 右边：7

最终完整树：

plaintext

```
         4
       /   \
      1     6
       \   / \
        3 5   7
       /
      2
```

---

## 第五步：层序遍历（从上到下、从左到右）

一层一层数：

1. 第 0 层：4
2. 第 1 层：1, 6
3. 第 2 层：3, 5, 7
4. 第 3 层：2

连起来：

**4 1 6 3 5 7 2**

![](image/Pasted%20image%2020260413195907.png)

```c++
#include <iostream>
#include <vector>
#include <queue>
using namespace std;

// 二叉树节点结构
struct Node {
    int val;
    Node *left, *right;
    Node(int v) : val(v), left(NULL), right(NULL) {}
};

vector<int> post, in;
int n;

// 递归建树：后序[pl,pr]  中序[il,ir]
Node* build(int pl, int pr, int il, int ir) {
    if (pl > pr) return NULL;

    // 后序最后一个是根
    int root_val = post[pr];
    Node* root = new Node(root_val);

    // 找到中序里根的位置
    int k;
    for (k = il; k <= ir; k++) {
        if (in[k] == root_val) break;
    }

    int left_num = k - il;

    // 左子树、右子树
    root->left = build(pl, pl + left_num - 1, il, k - 1);
    root->right = build(pl + left_num, pr - 1, k + 1, ir);
/*
后序：`2 3 1 5 7 6 ④`
中序：`1 2 3 ④ 5 6 7`
*/
    return root;
}

// 层序遍历 BFS
void levelOrder(Node* root) {
    queue<Node*> q;
    q.push(root);
    bool first = true;

    while (!q.empty()) {
        Node* cur = q.front();
        q.pop();

        if (!first) cout << " ";
        first = false;
        cout << cur->val;

        if (cur->left) q.push(cur->left);
        if (cur->right) q.push(cur->right);
    }
}

int main() {
    cin >> n;
    post.resize(n);
    in.resize(n);

    for (int i = 0; i < n; i++) cin >> post[i];
    for (int i = 0; i < n; i++) cin >> in[i];

    Node* root = build(0, n - 1, 0, n - 1);
    levelOrder(root);

    return 0;
}
```
**其实这里发现，我们已经把这棵树 构造出来了！，所以 其 前序 | 中序 | 后序 遍历易得

## 模板【给定普通二叉树后序 + 中序 - 还原整颗树！】

**关键是理解记忆 build 函数**
1. 后序的最后一个是根。即a[pr] 是根，我们先将他拿出来，创一个新节点放进去
2. 后面用递归接上这个根的左右子树！
3. 遍历中序数组，找到我们需要的根的位置 k
4. 计算中序数组中，根左边的节点数量 cnt。 cnt = k - il
5. 对于中序，我们将根左边范围内的 传到  左子树中[il, k - 1]
6.              ， 我们将根右边范围 的传到 右子树中[k + 1, ir]
7. 对于后序，我们将给定的后序 范围内的前cnt个数 传到左子树中
8.               ，我们将                               剩下的数 传到右子树中
9. 最后别忘了一开始的递归边界：pl > pr

```c++
#include<bits/stdc++.h>
using namespace std;

struct node {
    int val;
    node* left, *right;
    
    node(int v) : val(v), left(NULL), right(NULL) {}
};

vector<int> post, in;
int n;

// 递归建树：后序[pl,pr]  中序[il,ir]
node * build(int pl, int pr, int il, int ir) {
    if (pl > pr) return NULL;

    int root_val = post[pr];
    node * root = new node(root_val);

    int k;
    for (k = il; k <= ir; k++) {
        if (in[k] == root_val) break;
    }

    int cnt = k - il;

    root->left = build(pl, pl + cnt - 1, il, k - 1);
    root->right = build(pl + cnt, pr - 1, k + 1, ir);

    return root;
}



void ap() {
    cin >> n;
    post.resize(n);
    in.resize(n);
    for (int i = 0; i < n; i++) cin >> post[i];
    for (int i = 0; i < n; i++) cin >> in[i];

    node *root = build(0, n - 1, 0, n - 1);
    // 就可以得到想要的遍历操作了！
    
    return ;
}

signed main () {
    int t; cin >> t;
    while (t--) ap();

    return 0;
}
```

## 再扩展！前序 + 中序 & 前序 + 后序

### 前序 + 中序
其实这个思路和 后序 + 中序 极其类似。
- 我们知道前序的第一个就是根，后面是左子树 + 右子树。那么我们可以先把根拿出来，创新节点。再通过递归接上其 左右子树
- 我们拿到根后，在中序中遍历查询根的位置 k
- 然后根据传进来的中序的左端点，我们拿到了左子树的节点数量 cnt，同时右子树的节点数量、
- 最后根据左右子树的节点数量，我们把左右端点传进去就可以了

### 前序 + 后序
这个和前面两个的大致思路是一样的，只是找的 根 的位置不一样
- 前序的第一个就是根，后面是左子树 + 右子树。我们拿出根，创建新节点
- 这里我们仍然是需要找到cnt的，那么如何操作呢？
- 小巧思，我们拿到前序中根的下一个位置，这个为左子树的根，
- 然后我们遍历后序查询这个根的位置 k，那么左子树的cnt = k - pl + 1
- 接下来同理
- 所以这里的关键和巧妙之处，就在于我们不再是找当前的根的位置了，因为这个根的位置其实已经是确定的，并且没有作用。所以我们换而找下一个子树的根，借此来找到子树的 cnt

## 扩展练习 | 二叉树的遍历

[L2-3 自然倍树 - 热身赛7](https://pintia.cn/problem-sets/2042978673330536448/exam/problems/type/7?problemSetProblemId=2042978673577992202)
![](image/Pasted%20image%2020260413195750.png)
```c++
#include<bits/stdc++.h>
using namespace std;

struct node {
    int val;
    node* left, *right;
    
    node(int v) : val(v), left(NULL), right(NULL) {}
};

vector<int> post, in;
int n;

node * build(int pl, int pr, int il, int ir) {
    if (pl > pr) return NULL;

    int root_val = post[pr];
    node * root = new node(root_val);

    int k;
    for (k = il; k <= ir; k++) {
        if (in[k] == root_val) break;
    }

    int cnt = k - il;

    root->left = build(pl, pl + cnt - 1, il, k - 1);
    root->right = build(pl + cnt, pr - 1, k + 1, ir);

    return root;
}

void final_check(node *root) {
    queue<node*> q;
    q.push(root);
    int k = 1;

    bool ok = true;
    while (!q.empty()) {
        int sz = q.size();
        for (int i = 0; i < sz; i++) {
            auto cur = q.front();
            int x = cur->val;
            q.pop();

            if (x % k != 0) {
                ok = false;
                break;
            }

            if (cur->left) q.push(cur->left);
            if (cur->right) q.push(cur->right);
        }
        k++;
        if (!ok) {
            cout << 0 << endl;
            break;
        }
    }
    if (ok) {
        cout << 1 << endl;
    }
}

void ap() {
    cin >> n;
    post.resize(n);
    in.resize(n);
    for (int i = 0; i < n; i++) cin >> post[i];
    for (int i = 0; i < n; i++) cin >> in[i];

    node *root = build(0, n - 1, 0, n - 1);
    final_check(root);
    
    return ;
}

signed main () {
    int t; cin >> t;
    while (t--) ap();

    return 0;
}
```


# 给定完全二叉树的层序遍历 还原二叉树

[L3-1 是不是堆 - 热身赛7](https://pintia.cn/problem-sets/2042978673330536448/exam/problems/type/7?problemSetProblemId=2042978673577992204)

## 孩子节点 下标公式

：`左孩子 = 2*i + 1`，`右孩子 = 2*i + 2`

只要满足：
1. 用**数组**存二叉树
2. 按照**层序遍历**的顺序放节点（从上到下、从左到右）
3. 根节点放在下标 **0** 的位置

那么：

**任何二叉树** 都满足：
- 左孩子 = 2*i + 1
- 右孩子 = 2*i + 2

**适用条件只有 3 个：
1. **数组存储**
2. **层序遍历顺序存放**
3. **根节点在下标 0**

和是不是完全二叉树没有关系！
完全二叉树只是**最适合、最常用**这种存储方式而已。


## 一眼看懂为什么

**左孩子 = 2i + 1**

**右孩子 = 2i + 2**

### 直观图（数组下标 0 开头）

plaintext

```
层数从上往下数：

第 0 层（根）
        i = 0
      /     \
第 1 层   i=1     i=2
        /  \     /  \
第 2 层 i=3  i=4 i=5  i=6
      /  \
第 3 层 i=7 ...
```

---

**对应数组**

`[ 0, 1, 2, 3, 4, 5, 6, 7 ]`

下标：0 1 2 3 4 5 6 7

### 为什么是 2i+1？一眼原理

每一层节点数都是**上一层的 2 倍**。

- 第 0 层：1 个
- 第 1 层：2 个
- 第 2 层：4 个
- 第 3 层：8 个
    
    ……

所以：

**到第 i 个节点为止，前面所有节点总数 ≈ 2i**

于是它的**左孩子紧接着排在 2i 后面**，就是 **2i+1**。

## 扩展练习 | 层序遍历 得到 后序遍历

[L3-1 是不是堆 - 热身赛7](https://pintia.cn/problem-sets/2042978673330536448/exam/problems/type/7?problemSetProblemId=2042978673577992204)
![](image/Pasted%20image%2020260413202324.png)
```c++
#include<bits/stdc++.h>
using namespace std;

//后序遍历：左子树，右子树，根。递归放到ans数组中
//层序遍历得到的数组，我们相当于 自顶向下 递归 每一个根
void ap_post(vector<int>& a, int i, vector<int>& ans) {
    int n = a.size();
    if (i >= n) return;

    ap_post(a, i * 2 + 1, ans);
    ap_post(a, i * 2 + 2, ans);
    ans.push_back(a[i]);
}

int t, n;
void ap() {
    vector<int> a(n); for (int i = 0; i < n; i++) cin >> a[i];
    
    bool max_heap = true, min_heap = true;
    for (int i = 0; i <= n / 2 - 1; i++) {
        int left = i * 2 + 1;
        int right = i * 2 + 2;

        if (left < n && a[i] < a[left]) max_heap = false;
        if (right < n && a[i] < a[right]) max_heap = false;

        if (left < n && a[i] > a[left]) min_heap = false;
        if (right < n && a[i] > a[right]) min_heap = false;
    }

    if (max_heap) {
        cout << "Max Heap" << endl;
    }
    if (min_heap) {
        cout << "Min Heap" << endl;
    }
    if (!max_heap && !min_heap) {
        cout << "Not Heap" << endl;
    }

    vector<int> ans;
    ap_post(a, 0, ans);

    for (int i = 0; i < ans.size(); i++) {
        if (i > 0) cout << " ";
        cout << ans[i];
    }
    
    return ;
}
signed main () {
    cin >> t >> n;
    while (t--) {
        ap();
        if (t != 0) cout << endl;
    }
    return 0;
}
```
tips:
- 这里的ans数组可以放在全局变量，同时递归函数就可以省去传ans数组。但是每一次递归 ap_post 之前要记得 ans.clear();
- 但是这里的层序遍历数组一定要传的，因为我们要用到 put根节点的值 的操作
 - **这里的传孩子节点，可以可视化为一层一层地 往 子树 里边递，直到递到NULL，则 归——
   将我们的子问题从小开始 填 ！put!** 最终填完就得到我们想要的数组

其实这里的heap很简单，只是为了引出 孩子节点的公式，所以我们可以把 层序 得到 后序 精炼出来
### 层序遍历 还原 后序遍历
```c++
#include<bits/stdc++.h>
using namespace std;

//后序遍历：左子树，右子树，根。递归放到ans数组中
//层序遍历得到的数组，我们相当于 自顶向下 递归 每一个根
vector<int> ans;
void ap_post(vector<int>& a, int i) {//所以为了更好理解，我们大可以把函数名字改为put_tree
    int n = a.size();
    if (i >= n) return;

    ap_post(a, i * 2 + 1);//放 左子树
    ap_post(a, i * 2 + 2);//放 右子树
    ans.push_back(a[i]);//放 根
}

int t, n;
void ap() {
    vector<int> a(n); for (int i = 0; i < n; i++) cin >> a[i];

    // vector<int> ans;
    ans.clear();
    ap_post(a, 0);
    
    for (int i = 0; i < ans.size(); i++) {
        if (i > 0) cout << " ";
        cout << ans[i];
    }
    
    return ;
}
signed main () {
    cin >> t >> n;
    while (t--) {
        ap();
        if (t != 0) cout << endl;
    }
    return 0;
}
```


由上面这个再进行扩展，我们便可以得到

# 完全二叉树 的 层序遍历 还原 前序 | 中序 | 后序 遍历
## 统一规律？ put位置！

```
前序：根 → 左 → 右
中序：左 → 根 → 右
后序：左 → 右 → 根
```
## 前序
根 → 左 → 右
```c++
vector<int> ans;

void pre_put(vector<int>& a, int i) {
    int n = a.size();
    if (i >= n) return;

    ans.push_back(a[i]);
    pre_put(a, i * 2 + 1);
    pre_put(a, i * 2 + 2);
}

void ap() {
    vector<int> a(n); for (int i = 0; i < n; i++) cin >> a[i];

    // vector<int> ans;
    ans.clear();
    pre_put(a, 0);
    
    for (int i = 0; i < ans.size(); i++) {
        if (i > 0) cout << " ";
        cout << ans[i];
    }
    
    return ;
}

```

## 中序
左 → 根 → 右
```c++
vector<int> ans;

void in_put(vector<int>& a, int i) {
    int n = a.size();
    if (i >= n) return;

    in_put(a, i * 2 + 1);
    ans.push_back(a[i]);
    in_put(a, i * 2 + 2);
}

void ap() {
    vector<int> a(n); for (int i = 0; i < n; i++) cin >> a[i];

    // vector<int> ans;
    ans.clear();
    in_put(a, 0);
    
    for (int i = 0; i < ans.size(); i++) {
        if (i > 0) cout << " ";
        cout << ans[i];
    }
    
    return ;
}

```

## 后序
左 → 右 → 根
```c++
vector<int> ans;

void post_put(vector<int>& a, int i) {
    int n = a.size();
    if (i >= n) return;

    post_put(a, i * 2 + 1);
    post_put(a, i * 2 + 2);
    ans.push_back(a[i]);
}

void ap() {
    vector<int> a(n); for (int i = 0; i < n; i++) cin >> a[i];

    // vector<int> ans;
    ans.clear();
    post_put(a, 0);
    
    for (int i = 0; i < ans.size(); i++) {
        if (i > 0) cout << " ";
        cout << ans[i];
    }
    
    return ;
}

```



# 完全二叉树的后序 还原 层序

[L2-035 完全二叉树的层序遍历 - 团体程序设计天梯赛-练习集](https://pintia.cn/problem-sets/994805046380707840/exam/problems/type/7?problemSetProblemId=1336215880692482058&page=1)
![](image/Pasted%20image%2020260414192603.png)

```c++
#include<bits/stdc++.h>
using namespace std;

const int MAXN = 35;  
vector<int> post;    
int ans[MAXN];     
int n;              
int idx;            


void build(int pos) {
    if (pos > n) return;  // 超出节点数，直接返回
    build(2 * pos);       // 先构建左子树
    build(2 * pos + 1);   // 再构建右子树
    ans[pos] = post[idx++]; // 最后赋值根节点（后序特性）
}

int main() {
    cin >> n;
    post.resize(n);
    for (int i = 0; i < n; i++) {
        cin >> post[i];
    }
    idx = 0;       
    build(1);       
    
    //1~n
    for (int i = 1; i <= n; i++) {
        if (i == 1) cout << ans[i];
        else cout << " " << ans[i];
    }
    cout << endl;
    return 0;
}
```

## 扩展 | 给定完全二叉树 的 前序 | 中序 | 后序 还原 层序遍历数组  

因为：
- 完全二叉树的**结构位置固定**（`2i+1`、`2i+2`）
- 我们只是在**按不同顺序填数字**
- 填的顺序 = 遍历顺序

**结构不变，顺序由赋值位置决定。**

**所以我们只需要修改遍历的顺序， 当然，这里的数组就不再是post了**


### 前序遍历：根 → 左 → 右

```c++
ap_pre[pos] = pre[idx++]; // 先根
build(2*pos+1);          // 再左
build(2*pos+2);          // 最后右
```

### 中序遍历：左 → 根 → 右

```c++
build(2*pos+1);          // 先左
ap_in[pos] = in[idx++]; // 再根
build(2*pos+2);          // 最后右
```

### 后序遍历：左 → 右 → 根

```c++
build(2*pos+1);          // 先左
build(2*pos+2);          // 再右
ap_post[pos] = post[idx++]; // 最后根
```

tips: 如果我们把完全二叉树的根放在0，那么左子树为2* i +1,右子树为2 * i + 2
						  放在1， 那么左子树为2 * i, 右子树为2 * i + 1
所以这里的下标可以根据我们放根的位置进行调整

**把这个总结起来就是模板了！


## 模板【完全二叉树！只给其 前 | 中 | 后 序，要求得 层序遍历数组】

```c++
#include<bits/stdc++.h>
using namespace std;

const int MAXN = 35;
int ans[MAXN];//层序遍历

int n;
vector<int> seq;

void build_post(int pos, int& idx) {//没有全局变量就要引用！
    if (pos >= n) return;
    build_post(2 * pos + 1, idx);
    build_post(2 * pos + 2, idx);
    ans[pos] = seq[idx++]; 
}

void build_pre(int pos, int& idx) {
    if (pos >= n) return;
    ans[pos] = seq[idx++];  
    build_pre(2 * pos + 1, idx);
    build_pre(2 * pos + 2, idx);
}

void build_in(int pos, int& idx) {
    if (pos >= n) return;
    build_in(2 * pos + 1, idx);
    ans[pos] = seq[idx++];   
    build_in(2 * pos + 2, idx);
}

int main() {
    cin >> n;
    seq.resize(n);//写全局变量的话，先读再开大小。即上面不用开大小
    
    for (int i = 0; i < n; ++i) {
        cin >> seq[i];
    }

    int idx = 0;
    
    build_post(0, idx); 
    // build_pre(0, idx); 
    // build_in(0, idx);  

    for (int i = 0; i < n; ++i) {
        if (i > 0) cout << " ";
        cout << ans[i];
    }
    cout << endl;
    
    return 0;
}
```

**总结：

对于**固定结构的完全二叉树**（数组 0 下标）：

- 给**前序序列** → 用 `根左右` 填数组
- 给**中序序列** → 用 `左根右` 填数组
- 给**后序序列** → 用 `左右根` 填数组


**关键
**这个 build 函数的本质不是 “建树”，
而是 “按遍历顺序往固定位置填数”。**
只要结构固定（完全二叉树），
**前 / 中 / 后序都能用这套模板，只换一行代码。**


## 联系

到这里，我们发现，给定 前中后序，求层序遍历 的代码和 给定层序遍历，求前中后序的代码非常相似！

我们可以放在一起看看
这里我们以前序为例

层序 -> 前序
```c++
vector<int> ans;

void pre_put(vector<int>& a, int i) {
    int n = a.size();
    if (i >= n) return;

    ans.push_back(a[i]);
    pre_put(a, i * 2 + 1);
    pre_put(a, i * 2 + 2);
}

void ap() {
    vector<int> a(n); for (int i = 0; i < n; i++) cin >> a[i];

    // vector<int> ans;
    ans.clear();
    pre_put(a, 0);
    
    for (int i = 0; i < ans.size(); i++) {
        if (i > 0) cout << " ";
        cout << ans[i];
    }
    
    return ;
}
```

前序 -> 层序
```c++
const int MAXN = 35;
int ans[MAXN];//层序遍历

int n;
vector<int> seq;

void build_pre(int pos, int& idx) {
    if (pos >= n) return;
    ans[pos] = seq[idx++];  
    build_pre(2 * pos + 1, idx);
    build_pre(2 * pos + 2, idx);
}

int main() {
    cin >> n;
    seq.resize(n);//写全局变量的话，先读再开大小。即上面不用开大小
    
    for (int i = 0; i < n; ++i) {
        cin >> seq[i];
    }

    int idx = 0;
    
    build_pre(0, idx); 

    for (int i = 0; i < n; ++i) {
        if (i > 0) cout << " ";
        cout << ans[i];
    }
    cout << endl;
    
    return 0;
}
```


**我们可以发现，这两个代码非常相似！！**
**再简化一下, 把n和给定的数组放到全局变量，**
```c++
///层序 -> 前序
int idx = 0;
void pre_put(int i) {
    int n = a.size();
    if (i >= n) return;

    //ans.push_back(a[i]);
    ans[idx++] = a[i];//这里的数组a是层序遍历的数组
    pre_put(2 * i + 1);
    pre_put(2 * i + 2);
}
```

```c++
//前序 -> 层序
int idx = 0;
void build_pre(int i) {
    if (i >= n) return;
    
    ans[i] = seq[idx++];  
    build_pre(2 * i + 1);
    build_pre(2 * i + 2);
}
```

我们可以发现，唯一的不同就是ans[idx++] = a[i]; 和 ans[i] = seq[idx++];  
这两个其实正好是相反的
但是他们的共同点都是 在层序遍历上 用idx记录位置

前序 -> 层序
i下标是位置，是层序遍历中 这个值的位置，所以i要辨认ans，而idx指代的是遍历的顺序，代表的是顺序遍历中 当前遍历到的位置

## 用树的语言翻译：

### **i = 当前节点 root**

### **2i+1 = root 的左孩子位置**

### **2i+2 = root 的右孩子位置**

### **if (i>= n) return = 这个位置不存在节点**

对于 层序 -> 前序
我们可以把 i 当作节点 root
，2i + 1 当作左子树，root->left
   2i + 2当作右子树，root->right
   if(i >= n ) return ; 当作空节点， if (root == NULL) return;
看成是，读到了这个节点，把这个节点的值拿出来，按前序的顺序放在前序数组中

对于 前序 -> 层序
我们可以看成是 访问到这个节点，给这个节点赋值
**访问节点 i → 把值读出来，按前序顺序放进答案**
关键是因为前序顺序就是我们访问的顺序！！！

## **两个函数都在 “前序遍历一棵树”**

## **唯一区别：一个在读节点，一个在写节点。**

# 你可以这样统一理解：

### 不管是哪个函数

### 递归路线 = 前序遍历一棵树

### i = 正在访问的节点位置

### 2i+1 = 左孩子

### 2i+2 = 右孩子

---

### 层序 → 前序

**访问节点 i → 把值读出来，按前序顺序放进答案**

### 前序 → 层序

**访问节点 i → 把值写进去，按前序顺序填到节点里**



## 关联笔记
- [[树]] — 树基础
- [[Morris 中序遍历]] — Morris 中序遍历
