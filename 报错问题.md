
# 超时 TLE

## 位移溢出变成**负数**或者 `0` -> while无限循环

**位移运算开1LL**


```
while (a[i] >= a[i - 1] * (1 << k)) k++;
// 以及
while (a[i] * (1 << k) < a[i - 1]) k++;
```

当 $k$ 随着循环不断变大时，`(1 << k)` 会发生**位移溢出**：

1. 在 C++ 中，`1` 默认是 32 位有符号整型（`int`）。
    
2. 当 `k >= 31` 时，`1 << k` 就会溢出变成**负数**或者 `0`。
    
3. 一旦变成负数或 `0`：
    
    - `a[i - 1] * (1 << k)` 就会变成 `0` 或者负数。
        
    - 此时 `while (a[i] >= 负数)` 就会**永远成立**！
        
    - `k` 开始疯狂自增直到超出边界，引发死循环，从而导致了你看到的 **TLE**。
        

你可能会想：“我已经 `#define int long long` 了啊？”

但是在 C++ 中，字面量 `1` 依然会被优先当作 32 位 `int` 处理。必须写成 `1LL << k` 才能保证它是 `long long` 位移。但即便改成了 `1LL`，当 `k >= 62` 时它依然会溢出，治标不治本。


[P12836 [蓝桥杯 2025 国 B] 翻倍 - 洛谷](https://www.luogu.com.cn/problem/P12836#ide)
```c++
#include<bits/stdc++.h>
using namespace std;
#define int long long

int ksm(int a, int b) {
    int ans = 1;
    while (b) {
        if (b & 1) ans = ans * a;
        a = a * a;
        b >>= 1;
    }
    return ans;
}
//这题的关键在于，如果第一个数接近2^32，后面的数小，最坏的情况，每个数都要变成2的 32，33，34，，，1e5，显然会爆Longlong
signed main () {
    int n; cin >> n;
    vector<int> a(n);
    for (int & i : a) cin >> i;

    vector<int> c(n);
    for (int i = 1; i < n; i++) {
        if (a[i] >= a[i - 1] && c[i - 1] == 0) continue;

        
        if (a[i] >= a[i - 1]) {
            int k = 1;
            while (a[i] >= a[i - 1] * (1LL << k)) k++;
            k--;
            c[i] = max(0LL, c[i - 1] - k);
        } else {
            int k = 1;
            while (a[i] * (1LL << k) < a[i - 1]) k++;
            c[i] = c[i - 1] + k;
        }
    }

    int ans = 0;
    for (int x : c) ans += x;
    cout << ans;

    return 0;
}
```
