
# 任意次 交换问题 | 连通性
[D-小红的排序（easy）_牛客周赛 Round 140](https://ac.nowcoder.com/acm/contest/132940/D)
虽然贴的是easy版本，但是hard一样的代码就可以过
[E-小红的排序（hard）_牛客周赛 Round 140](https://ac.nowcoder.com/acm/contest/132940/E)
![[Pasted image 20260420194219.png]]

## 题意
给定一个n排列数组，以及两个整数x, y
问：任意swap(pi, pi+ x)，或者y， 可以使其变为升序排列吗？ Yes : No

那么我们可以发现，交换具有传递性，也就是只要两个位置有连通，不管是不是直接相连通，那么就可以交换，并且可以做到只改变这两个位置的值
```c++
#include <bits/stdc++.h>  
using namespace std;  
#define int long long
#define endl '\n'
const int mod = 1e9 + 7;
const int MOD = 998244353;

class UnionFind {
    vector<int> fa; // 代表元
    vector<int> sz; // 集合大小

public:
    int cc; // 连通块个数

    UnionFind(int n) : fa(n + 1), sz(n + 1, 1), cc(n) {
        // 一开始有 n 个集合 {0}, {1}, ..., {n-1}
        // 集合 i 的代表元是自己，大小为 1
         iota(fa.begin(), fa.end(), 0);
    }

    // 返回 x 所在集合的代表元
    // 同时做路径压缩，也就是把 x 所在集合中的所有元素的 fa 都改成代表元
    int find(int x) {
        // 如果 fa[x] == x，则表示 x 是代表元
        if (fa[x] != x) {
            fa[x] = find(fa[x]); // fa 改成代表元
        }
        return fa[x];
    }

    // 判断 x 和 y 是否在同一个集合
    bool is_same(int x, int y) {
        // 如果 x 的代表元和 y 的代表元相同，那么 x 和 y 就在同一个集合
        // 这就是代表元的作用：用来快速判断两个元素是否在同一个集合
        return find(x) == find(y);
    }

    // 把 from 所在集合合并到 to 所在集合中
    // 返回是否合并成功
    bool merge(int from, int to) {
        int x = find(from), y = find(to);
        if (x == y) { // from 和 to 在同一个集合，不做合并
            return false;
        }
        fa[x] = y; // 合并集合。修改后就可以认为 from 和 to 在同一个集合了
        sz[y] += sz[x]; // 更新集合大小（注意集合大小保存在代表元上）
        // 无需更新 sz[x]，因为我们不用 sz[x] 而是用 sz[find(x)] 获取集合大小，但 find(x) == y，我们不会再访问 sz[x]
        cc--; // 成功合并，连通块个数减一
        return true;
    }

    // 返回 x 所在集合的大小
    int get_size(int x) {
        return sz[find(x)]; // 集合大小保存在代表元上
    }
};

void solve() {
	int n, x, y; cin >> n >> x >> y;
    vector<int> p(n); for (int i = 0; i < n; i++) cin >> p[i];
    if (x == 1 || y == 1) {
        cout << "Yes" << endl;
        return ;
    }
    
    UnionFind uf(n);
    for (int i = 0; i < n; i++) {
    if (i + x < n) {
            uf.merge(i, i + x);
        }
    }

    for (int i = 0; i < n; i++) {
        if (i + y < n) {
            uf.merge(i, i + y);
        }
    }
//     for (int i = 0; i < x; i++) {
//         int last = i;
//         for (int j = i + x; j < n; j += x) {
//             uf.merge(last, j);
//             last = j;
//         }
//     }
    
    
//     for (int i = 0; i < y; i++) {
//         int last = i;
//         for (int j = i + y; j < n; j += y) {
//             uf.merge(last, j);
//             last = j;
//         }
//     }
    
    if (uf.cc == 1) {
        cout << "Yes" << endl;
        return ;
    }
    
    for (int i = 0; i < n; i++) {
        if (p[i] == i + 1) continue;
        
        if (uf.find(i) != uf.find(p[i] - 1)) {
            cout << "No" << endl;
            return;
        }
    }
    cout << "Yes" << endl;
    
	return ;
}

signed main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int T = 1;
	cin >> T;
	while (T--) {
		solve();
	} 
    return 0;
}
```

## 让模 x 同余 的位置连通的写法

tip:这里我们连通的是下标
```c++
	//这个代码需要学习，连通的是模x同余的位置。
	//更简洁
	for (int i = 0; i < n; i++) {
	    if (i + x < n) {
            uf.merge(i, i + x);
        }
    }

    for (int i = 0; i < n; i++) {
        if (i + y < n) {
            uf.merge(i, i + y);
        }
    }
```

```c++
	//这个代码是我画图时所呈现的
	//我们枚举起点，对每个起点都走一遍。观察发现最多只需要枚举x个起点
    for (int i = 0; i < x; i++) {
        int last = i;
        for (int j = i + x; j < n; j += x) {
            uf.merge(last, j);
            last = j;
        }
    }
    
    
    for (int i = 0; i < y; i++) {
        int last = i;
        for (int j = i + y; j < n; j += y) {
            uf.merge(last, j);
            last = j;
        }
    }
```

发现这题和新出的cf Div3中的c题一模一样
[Problem - C - Codeforces](https://codeforces.com/contest/2244/problem/C)

## 关联笔记
- [[图论]] — 并查集在图论中的应用（连通性）
- [[数据结构/并查集]] — 并查集专题
- [[数据结构/并查集/可持久化并查集]] — 可持久化并查集
- [[数据结构/并查集/可撤销并查集]] — 可撤销并查集
