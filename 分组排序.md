[1202. 交换字符串中的元素 - 力扣（LeetCode）](https://leetcode.cn/problems/smallest-string-with-swaps/description/)
![](image/Pasted%20image%2020260401164459.png)
```c++
class UnionFind {

    vector<int> fa; // 代表元

    vector<int> sz; // 集合大小

  

public:

    int cc; // 连通块个数

  

    UnionFind(int n) : fa(n), sz(n, 1), cc(n) {

        // 一开始有 n 个集合 {0}, {1}, ..., {n-1}

        // 集合 i 的代表元是自己，大小为 1

        ranges::iota(fa, 0); // iota(fa.begin(), fa.end(), 0);

    }

  

    // 返回 x 所在集合的代表元

    // 同时做路径压缩，也就是把 x 所在集合中的所有元素的 fa 都改成代表元

    int find(int x) {

        // 如果 fa[x] == x，则表示 x 是代表元

        if (fa[x] != x) {

            fa[x] = find(fa[x]); // fa 改成代表元

        }

        return fa[x];

    }

  

    // 判断 x 和 y 是否在同一个集合

    bool is_same(int x, int y) {

        // 如果 x 的代表元和 y 的代表元相同，那么 x 和 y 就在同一个集合

        // 这就是代表元的作用：用来快速判断两个元素是否在同一个集合

        return find(x) == find(y);

    }

  

    // 把 from 所在集合合并到 to 所在集合中

    // 返回是否合并成功

    bool merge(int from, int to) {

        int x = find(from), y = find(to);

        if (x == y) { // from 和 to 在同一个集合，不做合并

            return false;

        }

        fa[x] = y; // 合并集合。修改后就可以认为 from 和 to 在同一个集合了

        sz[y] += sz[x]; // 更新集合大小（注意集合大小保存在代表元上）

        // 无需更新 sz[x]，因为我们不用 sz[x] 而是用 sz[find(x)] 获取集合大小，但 find(x) == y，我们不会再访问 sz[x]

        cc--; // 成功合并，连通块个数减一

        return true;

    }

  

    // 返回 x 所在集合的大小

    int get_size(int x) {

        return sz[find(x)]; // 集合大小保存在代表元上

    }

};

class Solution {//如果可以交换的数组有交集，那么说明可以合并，其都可以交换。那么我们其实只需要连通分量中的下标对应的字符按字典序最小排列就好了

public:

    string smallestStringWithSwaps(string s, vector<vector<int>>& pairs) {

        int n = s.size();

        UnionFind uf(n);//n个集合

        for (auto p : pairs) {

            uf.merge(p[0], p[1]);

        }

  

        //分组排序

        //遍历s，对于每一个下标，我们通过find找到这一组的头头，其作为map的键，将s[i]放入到map中，

        //然后再遍历map，对其vec进行降序排序，

        //最后从左到右遍历下标，对于每一个下标，找到其对应的头头，将头头的vec中的back弹出，作为当前的字符

        map<int, vector<int>> op;

        for (int i = 0; i < n; i++) {

            op[uf.find(i)].push_back(s[i]);

        }

        for (auto& [x, vec] : op) {

            sort(vec.begin(), vec.end(), greater<int>());

        }

        for (int i = 0; i < n; i++) {

            int x = uf.find(i);

            s[i] = op[x].back();

            op[x].pop_back();

        }

        return s;

    }

};
```

## 关联笔记
- [并查集](并查集.md) — 分组排序依赖连通分量
