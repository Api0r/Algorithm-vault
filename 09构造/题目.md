
# 1.

https://codeforces.com/contest/2259/problem/D

三个MEX集合 A B C
假设我们让C 为0，那么我们可以将任意的垃圾数字扔到集合C中，只要其不为0

这样假设需要满足什么呢？
`MEXA + MEXB >= 2 * max(MEXA, MEXB)`
注意到，当`MEXA = MEXB` 时总是成立的

那么如何使`MEXA = MEXB`呢？
我们有一个很好的条件，可以将除0以外的任意数都扔到集合C中
假设有c个数x，我们可以分一个到A，分一个到B，其余都到C
如果`c = 1` ，那么就只分到C
想到这里，其实只要`x != 1` , 那么我们就将所有的x都加到C中
那么只有一种情况，当`cnt0 = 1` 时，无法构造，其他情况都可以

```c++
#include <bits/stdc++.h>  
using namespace std;  
#define int long long
#define endl '\n'
const int mod = 1e9 + 7;
const int MOD = 998244353;

void ap() {
	int n; cin >> n;
    vector<int> a(n);
    for (int & i : a) cin >> i;

    int cnt0 = 0;
    for (int & x : a) {
        if (x == 0) cnt0++;
    }

    if (cnt0 == 1) {
        cout << "NO" << endl;
        return;
    }

    map<int, vector<int>> op;
    for (int i = 0; i < n; i++) {
        int x = a[i];
        op[x].push_back(i);
    }

    vector<char> ans(n, '0');
    for (auto & [x, p] : op) {
        if (x == 0) {
            ans[p[0]] = 'A';
            ans[p[1]] = 'B';

            for (int i = 2; i < p.size(); i++) {
                ans[p[i]] = 'A';
            }
            continue;
        }

        //x > 1
        for (int i = 0; i < p.size(); i++) {
            ans[p[i]] = 'C';
        }

    }


    cout << "YES" << endl;
    for (auto & x : ans) {
        if (x == '0') x = 'A';
    }

    for (auto x : ans) cout << x;
    cout << endl;

	return ;
}

signed main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int T = 1;
	cin >> T;
	while (T--) {
		ap();
	} 
    return 0;
}
```


# 1.递归构造

https://codeforces.com/gym/106554/problem/D

```c++
#include <bits/stdc++.h>  
using namespace std;  
#define int long long
#define endl '\n'
const int mod = 1e9 + 7;
const int MOD = 998244353;

// 存储所有构造好的电梯路径
vector<vector<int>> elevators;

// 递归求解区间 [L, R] 内所有点对之间的直达覆盖
void build(int L, int R, int n) {
    int len = R - L + 1;
    if (len <= 1) return;

    if (len == 2) {
        // 只有 2 个点：覆盖边 (L, R)
        // 补齐全局端点 1 和 n
        vector<int> path;
        if (L != 1) path.push_back(1);
        path.push_back(L);
        path.push_back(R);
        if (R != n) path.push_back(n);
        elevators.push_back(path);
        return;
    }

    if (len == 3) {
        // 3 个点：L, L+1, R
        // 路径 1: 包含 (L, L+1) 和 (L+1, R)
        vector<int> p1;
        if (L != 1) p1.push_back(1);
        p1.push_back(L);
        p1.push_back(L + 1);
        p1.push_back(R);
        if (R != n) p1.push_back(n);
        elevators.push_back(p1);

        // 路径 2: 包含 (L, R)
        vector<int> p2;
        if (L != 1) p2.push_back(1);
        p2.push_back(L);
        p2.push_back(R);
        if (R != n) p2.push_back(n);
        elevators.push_back(p2);
        return;
    }

    // 1. 覆盖所有涉及 L 和 R 的边
    // 覆盖 (L, i) 和 (i, R)，其中 L < i < R
    for (int i = L + 1; i < R; i++) {
        vector<int> path;
        if (L != 1) path.push_back(1);
        path.push_back(L);
        path.push_back(i);
        path.push_back(R);
        if (R != n) path.push_back(n);
        elevators.push_back(path);
    }

    // 覆盖直连边 (L, R)
    vector<int> direct;
    if (L != 1) direct.push_back(1);
    direct.push_back(L);
    direct.push_back(R);
    if (R != n) direct.push_back(n);
    elevators.push_back(direct);

    // 2. 剥离外层 L 和 R，递归解决内部子区间 [L+1, R-1]
    build(L + 1, R - 1, n);
}

/*
f(k)
2 -> 1
3 -> 2
4 -> 4 -> 4 - 1 + f(2)
5 -> 6 -> 5 - 1 + f(3)
...
k -> k - 1 + f(k - 2);
*/

void ap() {
	int n; cin >> n;

    int dp[1005];
    dp[2] = 1;
    dp[3] = 2;
    for (int i = 4; i <= n; i++) {
        dp[i] = i - 1 + dp[i - 2];
    }

    cout << dp[n] << endl;
    
    build(1, n, n);

    // 快速输出所有电梯停靠层
    for (const auto& path : elevators) {
        for (int i = 0; i < (int)path.size(); i++) {
            cout << path[i] << (i + 1 == (int)path.size() ? "" : " ");
        }
        cout << "\n";
    }

	return ;
}

signed main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int T = 1;
//	cin >> T;
	while (T--) {
		ap();
	} 
    return 0;
}
```