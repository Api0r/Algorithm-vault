
# 1.

https://codeforces.com/contest/2259/problem/D

三个MEX集合 A B C
假设我们让C 为0，那么我们可以将任意的垃圾数字扔到集合C中，只要其不为0

这样假设需要满足什么呢？
`MEXA + MEXB >= 2 * max(MEXA, MEXB)`
注意到，当`MEXA = MEXB` 时总是成立的

那么如何使`MEXA = MEXB`呢？
我们有一个很好的条件，可以将除0以外的任意数都扔到集合C中
假设有c个数x，我们可以分一个到A，分一个到B，其余都到C
如果`c = 1` ，那么就只分到C
想到这里，其实只要`x != 1` , 那么我们就将所有的x都加到C中
那么只有一种情况，当`cnt0 = 1` 时，无法构造，其他情况都可以

```c++
#include <bits/stdc++.h>  
using namespace std;  
#define int long long
#define endl '\n'
const int mod = 1e9 + 7;
const int MOD = 998244353;

void ap() {
	int n; cin >> n;
    vector<int> a(n);
    for (int & i : a) cin >> i;

    int cnt0 = 0;
    for (int & x : a) {
        if (x == 0) cnt0++;
    }

    if (cnt0 == 1) {
        cout << "NO" << endl;
        return;
    }

    map<int, vector<int>> op;
    for (int i = 0; i < n; i++) {
        int x = a[i];
        op[x].push_back(i);
    }

    vector<char> ans(n, '0');
    for (auto & [x, p] : op) {
        if (x == 0) {
            ans[p[0]] = 'A';
            ans[p[1]] = 'B';

            for (int i = 2; i < p.size(); i++) {
                ans[p[i]] = 'A';
            }
            continue;
        }

        //x > 1
        for (int i = 0; i < p.size(); i++) {
            ans[p[i]] = 'C';
        }

    }


    cout << "YES" << endl;
    for (auto & x : ans) {
        if (x == '0') x = 'A';
    }

    for (auto x : ans) cout << x;
    cout << endl;

	return ;
}

signed main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int T = 1;
	cin >> T;
	while (T--) {
		ap();
	} 
    return 0;
}
```