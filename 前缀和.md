```
vector<int> pre(n + 1);
for (int i = 0; i < n; i++) {

	   pre[i + 1] = pre[i] + nums[i];
	   
}
```
子数组 [l,r) 的 和 为pre[r] - pre[l];
注意这里的l，r是对于原来数组中的，即不是pre数组中的对应的数字，即l，r是与原来数组中的下标
我们看一个例子：要求[0, 3) 中的子数组的和，我们需要求出[0, 2]的前缀和，减去0，这个便于我们理解pre数组整体右移一位，使第一位为0
更一般的，求[l, r) 子数组的和，我们需要用 对原数组中的[0, r - 1] 的前缀和 减去 [0, l - 1] 的前缀和。 对应到pre数组中， 就是 pre[r] - pre[l];因为pre数组整体右移了一位，所以对应得下标 加一

总结：对于原数组中的 [l, r) 的子数组和 ， 即相对于pre数组中的pre[r] - pre[l] . 注意l，r是原数组中的l，r

在很多时候，我们都可以用一个变量s取代pre数组
```c++
class Solution {
public:
    int findMaxLength(vector<int>& nums) {
        for (int& x : nums) {
            x = x ? 1 : -1; // 把 0 当作 -1
        }

        // 计算 nums 的前缀和
        int n = nums.size();
        vector<int> sum(n + 1);
        partial_sum(nums.begin(), nums.end(), sum.begin() + 1);

        unordered_map<int, int> pos;
        int ans = 0;
        for (int i = 0; i <= n; i++) {
            int s = sum[i];
            if (pos.contains(s)) {
                // 找到一对相等元素，下标分别为 pos[s] 和 i，距离为 i - pos[s]
                ans = max(ans, i - pos[s]);
            } else {
                pos[s] = i;
            }
        }
        return ans;
    }
};
```

```c++
class Solution {
public:
    int findMaxLength(vector<int>& nums) {
        // 前缀和数组的首项 0 相当于在 -1 下标
        unordered_map<int, int> pos = {{0, -1}};
        int ans = 0, s = 0;
        for (int i = 0; i < nums.size(); i++) {
            s += nums[i] ? 1 : -1;
            if (pos.contains(s)) {
                ans = max(ans, i - pos[s]);
            } else {
                pos[s] = i;
            }
        }
        return ans;
    }
};
```

# 二维前缀和
```c++
NumMatrix(vector<vector<int>>& matrix) {
        int m = matrix.size(), n = matrix[0].size();
        sum.resize(m + 1, vector<int>(n + 1));
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                sum[i + 1][j + 1] = sum[i + 1][j] + sum[i][j + 1] - sum[i][j] + matrix[i][j];
            }
        }
    }

    // 返回左上角在 (r1, c1)，右下角在 (r2, c2) 的子矩阵元素和
    int sumRegion(int r1, int c1, int r2, int c2) {
        return sum[r2 + 1][c2 + 1] - sum[r2 + 1][c1] - sum[r1][c2 + 1] + sum[r1][c1];
    }

```


## 求子矩阵的和为k的个数

[D - Count Subgrid Sum = K](https://atcoder.jp/contests/abc461/tasks/abc461_d)
我们可以预处理算出二维前缀和，那么只需要知道子矩阵的左上角在(r1, c1)，右下角 (r2, c2)，就可以O(1) 的计算出子矩阵的元素和

```c++
vector<string> a(n);
    for (auto & i : a) cin >> i;

    vector<vector<int>> sum(n + 1, vector<int> (m + 1));
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            sum[i + 1][j + 1] = sum[i + 1][j] + sum[i][j + 1] - sum[i][j] + (a[i][j] - '0');
        }
    }
```

但是这需要枚举四个点，时间复杂度是O($n^4$);

```c++
int cnt = 0;

// 1. 枚举左上角的行 r1
for (int r1 = 1; r1 <= n; r1++) {
    // 2. 枚举左上角的列 c1
    for (int c1 = 1; c1 <= m; c1++) {
        
        // 3. 枚举右下角的行 r2（右下角的行一定不能小于左上角的行）
        for (int r2 = r1; r2 <= n; r2++) {
            // 4. 枚举右下角的列 c2（右下角的列一定不能小于左上角的列）
            for (int c2 = c1; c2 <= m; c2++) {
                
                // 利用二维前缀和公式，直接算出当前子矩阵 [(r1, c1) 到 (r2, c2)] 的元素和
                int cur_sum = sum[r2][c2] - sum[r1 - 1][c2] - sum[r2][c1 - 1] + sum[r1 - 1][c1 - 1];
                
                // 如果区域和刚好等于 k，计数器加 1
                if (cur_sum == k) {
                    cnt++;
                }
                
            }
        }
        
    }
}
```

## 降维打击

锁死上下边界，把“二维矩阵”压缩成“一维数组”，再用哈希表瞬间找出答案。

我们暴力枚举上下两个行边界，一层遍历列

那么中间遍历列的过程就是一维数组 中 子数组和的问题，用map可以做到空间换时间

```c++
for (int r1 = 1; r1 <= n; r1++) {
        for (int r2 = r1; r2 <= n; r2++) {
            map<int, int> mp;
            mp[0] = 1;

            for (int c = 1; c <= m; c++) {
                int cur_sum = sum[r2][c] - sum[r1 - 1][c];

                if (mp.count(cur_sum - k)) {
                    cnt += mp[cur_sum - k];
                }

                mp[cur_sum]++;
            }
        }
    }
```

## 关联笔记
- [[差分数组]] — 差分是前缀和的逆运算
- [[位运算]] — 前缀异或和
- [[区间问题]] — 子数组和、区间问题
- [[map]] — 哈希表优化前缀和
